# 1차시 - AI & 기계학습 기초

## 1. AI, ML, DL의 정의

| 구분 | 정의 | 관계 |
|------|------|------|
| **AI (인공지능)** | 인간의 지능을 모방하는 기술의 총칭. 규칙 기반 시스템도 포함 | 가장 넓은 개념 |
| **ML (기계학습)** | 명시적 프로그래밍 없이 데이터로부터 패턴을 학습하는 AI의 하위 분야 | AI ⊃ ML |
| **DL (딥러닝)** | 다층 신경망(심층 신경망)을 사용하는 ML의 하위 분야. 특징을 자동으로 추출 | ML ⊃ DL |

> AI → ML → DL (포함 관계)

### 왜 이 구분이 중요한가?

- 전통 AI: 사람이 규칙을 직접 만듦 (if-else). 복잡한 문제에 한계
- ML: 데이터에서 규칙을 스스로 학습. 하지만 사람이 **어떤 특징(Feature)을 쓸지** 직접 설계해야 함
- DL: 특징 추출까지 자동으로 학습. 이미지, 텍스트 등 비정형 데이터에 강력

### 연결 포인트

> ML의 핵심 질문은 "데이터에서 어떻게 패턴을 찾을 것인가?"이고, 이 질문에 대한 답 중 하나가 **신경망(DL)**이다. 1차시에서 배우는 회귀, 분류, 클러스터링은 모두 ML의 대표적 **문제 유형**이며, 2차시의 신경망은 이 문제들을 풀기 위한 **모델 구조** 중 하나다.

---

## 2. 데이터 구성 요소 (지도학습)

| 용어 | 설명 | 표기 | 예시 |
|------|------|------|------|
| **특성 (Feature)** | 예측에 사용되는 설명 변수. 예측 함수 f의 input | X | 집 크기, 방 수, 위치 |
| **라벨 (Label)** | 맞춰야 하는 정답 (실제값) | y | 집 가격 |
| **예측값** | 모델이 출력한 추정값 | ŷ | 모델이 예측한 가격 |
| **데이터셋** | (Feature, Label) 쌍의 모음 | D | 훈련/검증/테스트 셋으로 분할 |

### 학습 유형

| 유형 | Label 유무 | 목표 | 대표 알고리즘 |
|------|-----------|------|-------------|
| **지도학습** | O | 입력→정답 매핑 학습 | 회귀, 분류, 신경망 |
| **비지도학습** | X | 데이터 내부 구조/패턴 발견 | 클러스터링, 차원 축소 |
| **강화학습** | X (보상 있음) | 보상을 최대화하는 행동 학습 | Q-Learning, PPO |

### 데이터 분할의 중요성

| 구분 | 역할 | 비율 (일반적) |
|------|------|-------------|
| **훈련 세트 (Train)** | 모델 학습에 사용 | 60~80% |
| **검증 세트 (Validation)** | 하이퍼파라미터 튜닝, 모델 선택 | 10~20% |
| **테스트 세트 (Test)** | 최종 성능 평가 (한 번만 사용) | 10~20% |

> 훈련 데이터로 성능을 측정하면 **과적합**된 모델도 잘해 보이므로, 반드시 학습에 사용하지 않은 데이터로 평가해야 한다. 이 개념은 2차시의 "훈련 vs 테스트 성능" 논의와 직결된다.

---

## 3. 회귀 (Regression)

입력 변수(X)와 출력 변수(Y) 사이의 관계를 함수로 근사하여 **연속적인 값**을 예측하는 방법

- **분류와의 차이**: 회귀는 연속값(가격, 온도), 분류는 범주(고양이/개, 스팸/정상)를 예측
- 2차시에서 배울 **선형회귀**가 가장 기본적인 회귀 모델

### 회귀 오류 측정 - 평균제곱오차 (MSE)

```
MSE = (1/n) × Σ(yᵢ - ŷᵢ)²
```

- 예측값(ŷ)과 실제값(y)의 차이를 제곱하여 평균낸 것
- 값이 작을수록 예측이 정확함
- **왜 제곱하는가?**: ①양수/음수 오차가 상쇄되는 것을 방지 ②큰 오차에 더 큰 페널티 부여

### MSE와 다른 지표들의 관계

| 지표 | 수식 | 특징 |
|------|------|------|
| **MSE** | (1/n)Σ(y-ŷ)² | 가장 기본. 제곱 단위라 해석이 어려울 수 있음 |
| **RMSE** | √MSE | MSE에 루트 → 원래 단위로 해석 가능 |
| **MAE** | (1/n)Σ\|y-ŷ\| | 절대값 사용. 이상치에 덜 민감 |
| **R²** | 1 - RSS/TSS | 설명력 비율 (0~1) |

---

## 4. 결정계수 R² (회귀 설명력)

**핵심 정의**: Label(y)의 분산 중에서 Feature(X)로 설명되는 비율

```
R² = 1 - (RSS / TSS)

RSS = Σ(yᵢ - ŷᵢ)²    ← 잔차제곱합: 모델이 설명하지 못한 변동
TSS = Σ(yᵢ - ȳ)²     ← 총제곱합: 데이터의 총 변동
```

### R²의 직관적 이해

| R² 값 | 의미 | 비유 |
|--------|------|------|
| **R² = 1** | 모델이 데이터를 완벽하게 설명 | 모든 점이 회귀선 위에 있음 |
| **R² = 0.8** | 변동의 80%를 모델이 설명 | 꽤 좋은 모델 |
| **R² = 0** | 모델이 평균 예측과 동일한 수준 | 모델이 쓸모없음 |
| **R² < 0** | 평균보다도 못한 예측 | 모델이 오히려 해로움 |

> "평균만 쓰는 단순한 예측"보다 모델이 실제값을 **얼마나 더 잘 맞추는지**를 0~1 사이 값으로 나타냄

### R²와 RSS의 관계

- RSS는 2차시에서 배울 **최소제곱법**의 최적화 대상이다
- 최소제곱법으로 RSS를 줄이면 → R²가 올라간다
- 즉 **RSS 최소화 = R² 최대화 = 좋은 회귀 모델**

---

## 5. 혼동행렬 (Confusion Matrix)

분류 모델의 예측 결과를 실제값과 비교해 정리한 2×2 표

|  | 예측: Positive | 예측: Negative |
|--|---------------|---------------|
| **실제: Positive** | TP (True Positive) 정탐 | FN (False Negative) 미탐 |
| **실제: Negative** | FP (False Positive) 오탐 | TN (True Negative) 정탐 |

### 각 칸의 의미 (암 검진 예시)

| 용어 | 의미 | 예시 |
|------|------|------|
| **TP** | 양성을 양성으로 정확히 예측 | 암 환자를 암이라고 진단 |
| **TN** | 음성을 음성으로 정확히 예측 | 정상인을 정상이라고 진단 |
| **FP** | 음성을 양성으로 잘못 예측 (오탐) | 정상인을 암이라고 진단 |
| **FN** | 양성을 음성으로 잘못 예측 (미탐) | 암 환자를 정상이라고 진단 |

### 주요 성능 지표

| 지표 | 수식 | 의미 | 중요한 상황 |
|------|------|------|-----------|
| **정확도 (Accuracy)** | (TP+TN) / 전체 | 전체 중 맞춘 비율 | 클래스 균형일 때 |
| **정밀도 (Precision)** | TP / (TP+FP) | Positive 예측 중 실제 Positive | 오탐을 줄여야 할 때 (스팸 필터) |
| **재현율 (Recall)** | TP / (TP+FN) | 실제 Positive 중 맞춘 비율 | 미탐을 줄여야 할 때 (암 진단) |
| **F1 Score** | 2×(P×R)/(P+R) | 정밀도와 재현율의 **조화평균** | 둘 다 중요할 때 |

### 정확도의 함정

> 불균형 데이터 (양성 1%, 음성 99%)에서는 전부 음성이라고 해도 정확도가 99%로 보임
> → 실제로는 양성을 하나도 못 잡는 무용한 모델
> → 그래서 Precision, Recall, F1 Score를 **함께** 봐야 함

### Precision vs Recall 트레이드오프

- Precision을 높이려면 → 확실한 것만 Positive로 예측 → Recall 하락
- Recall을 높이려면 → 조금이라도 의심되면 Positive → Precision 하락
- **F1 Score**는 이 둘의 균형을 잡아주는 지표

### 연결 포인트

> 혼동행렬은 **분류 모델**의 성능 지표이고, 2차시의 **로지스틱 회귀**가 대표적인 이진 분류 모델이다. 3차시 NLP에서도 감정 분류 등의 성능을 혼동행렬로 평가한다.

---

## 6. 오버피팅 & 언더피팅

| 구분 | 설명 | 훈련 성능 | 테스트 성능 | 원인 |
|------|------|----------|-----------|------|
| **오버피팅** | 훈련 데이터에 과도하게 맞춤 | 높음 | 낮음 | 모델 너무 복잡, 데이터 부족 |
| **언더피팅** | 데이터의 패턴조차 포착 못함 | 낮음 | 낮음 | 모델 너무 단순, 학습 부족 |
| **적절한 학습** | 훈련과 테스트 모두 좋음 | 높음 | 높음 | 모델 복잡도와 데이터가 적절 |

### 오버피팅 비유

- 시험 문제를 **통째로 외워서** 같은 문제는 맞추지만 새 문제는 못 푸는 것
- 모델이 훈련 데이터의 **노이즈까지 학습**해버림

### 해결 방법들

| 방법 | 설명 |
|------|------|
| **k겹 교차 검증** | 데이터를 k개로 나눠 k번 반복 학습/검증 |
| **정규화 (Regularization)** | 모델 복잡도에 페널티 부여 |
| **드롭아웃 (Dropout)** | 신경망에서 일부 노드를 랜덤하게 꺼서 학습 |
| **조기 종료 (Early Stopping)** | 검증 성능이 나빠지기 시작하면 학습 중단 |
| **데이터 증강** | 데이터를 변형/추가하여 양 늘리기 |

### k겹 교차 검증 (k-Fold Cross Validation)

```
데이터를 5등분 (k=5):
[1][2][3][4][5]

1회차: [1]=검증, [2][3][4][5]=훈련
2회차: [2]=검증, [1][3][4][5]=훈련
3회차: [3]=검증, [1][2][4][5]=훈련
...
→ 5번의 성능을 평균내어 최종 평가
```

- 모든 데이터가 한 번씩 검증에 사용됨
- 적은 데이터에서도 신뢰성 있는 성능 평가 가능

---

## 7. 클러스터링 (비지도학습)

클러스터: 같은 도메인의 데이터를 군집화한 것. **집단 내부는 유사, 집단 간에는 상이**

### 지도학습과의 차이

| 지도학습 (분류) | 비지도학습 (클러스터링) |
|---------------|---------------------|
| 정답 라벨이 있음 | 라벨 없음 |
| "이 데이터는 A 클래스다" | "비슷한 데이터끼리 묶어라" |
| 새 데이터의 클래스 예측 | 데이터 구조 탐색/발견 |

### k-means 알고리즘

- 클러스터 개수(K)를 **사전에 지정**
- 목표: 클러스터 내 거리/차이의 합이 **최소**가 되도록 분할
- K개 군집이 서로 겹치지 않고, 모든 데이터가 정확히 하나의 군집에만 소속

**알고리즘 순서**:
1. **초기화**: 관측치들에 무작위로 1~K 클러스터를 임시 부여
2. **반복** (할당이 더 이상 바뀌지 않을 때까지):
   - 각 클러스터의 **중심(평균)** 계산
   - 관측치를 **가장 가까운 중심**의 클러스터에 재할당
3. 수렴: 중심점과의 평균 거리가 계속 줄어 안정화

**주의사항**:
- 항상 최적해를 보장하지 않음 (초기값에 따라 결과 달라짐)
- 여러 번 다른 초기값으로 시도하여 가장 좋은 결과 선택
- K를 사전에 정해야 한다는 한계

### 계층적 군집 (Hierarchical Clustering)

- 군집 개수를 **정해놓지 않음** (k-means와의 핵심 차이)
- 전체 구조를 **덴드로그램**(나무형 다이어그램)으로 제공
- 원하는 높이에서 잘라 군집 수 결정 가능
- 방식: 가장 가까운 데이터/군집끼리 순차적으로 합침 (병합적 방식)

### k-means vs 계층적 군집 비교

| k-means | 계층적 군집 |
|---------|-----------|
| K를 사전에 지정 | K 지정 불필요 |
| 빠름 (대규모 데이터에 적합) | 느림 (소규모에 적합) |
| 구형(spherical) 클러스터에 적합 | 다양한 형태 가능 |
| 결과가 초기값에 의존 | 결정적 결과 |

---

## 차시 연결 요약

> 1차시는 ML의 **큰 그림**을 잡는 단계다.
> - 회귀/분류의 개념과 평가 지표(MSE, R², 혼동행렬)는 2차시의 **선형회귀, 로지스틱 회귀**를 이해하는 기초가 된다.
> - 오버피팅/데이터 분할 개념은 이후 모든 차시에서 모델을 평가할 때 반복적으로 등장한다.
> - 비지도학습(클러스터링)은 지도학습과 대비되는 개념으로, 라벨 없는 데이터 활용법을 보여준다.
