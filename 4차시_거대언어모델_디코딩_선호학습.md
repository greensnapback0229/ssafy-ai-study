# 4차시 - Decoder 모델, 디코딩 알고리즘, LLM 평가, 선호 학습

> 3차시에서 Transformer의 Encoder(BERT)를 배웠다면, 4차시는 **Decoder 쪽**을 다룬다. GPT 같은 Decoder 모델이 어떻게 텍스트를 생성하는지, 생성 시 어떤 디코딩 전략을 쓰는지, LLM을 어떻게 평가하고 사람의 선호에 맞게 정렬하는지를 학습한다.

## 1. Decoder 모델 (GPT 계열)

### Encoder vs Decoder 모델

3차시에서 Transformer는 Encoder + Decoder 구조라고 배웠다. 실전에서는 **한쪽만** 사용하는 경우가 많다:

| 모델 | 구조 | 방향 | 주요 작업 | 대표 |
|------|------|------|----------|------|
| **Encoder 모델** | Transformer Encoder만 | 양방향 | 이해/분류 (감정 분석, NER) | BERT |
| **Decoder 모델** | Transformer Decoder만 | 단방향 (왼→오) | 텍스트 **생성** | GPT, LLaMA |
| **Encoder-Decoder** | 둘 다 사용 | 양방향→단방향 | 번역, 요약 | T5, BART |

### Decoder 모델의 작동 원리

**자기회귀적(Autoregressive) 생성**: 이전에 생성한 토큰들을 입력으로 받아 **다음 토큰 하나**를 예측하는 것을 반복

```
입력:  "오늘 날씨가"
1단계: "오늘 날씨가" → 모델 → "좋다" (다음 토큰 예측)
2단계: "오늘 날씨가 좋다" → 모델 → "고"
3단계: "오늘 날씨가 좋다고" → 모델 → "합니다"
4단계: "오늘 날씨가 좋다고 합니다" → 모델 → [종료]
```

### 왜 Masked Self-Attention을 쓰는가?

- 생성 시점에서 **아직 나오지 않은 미래 토큰**을 볼 수 없음
- Mask로 미래 위치를 가려서 **현재까지의 토큰만** 참고하도록 강제

```
"오늘 날씨가 좋다"에서 각 토큰이 볼 수 있는 범위:
"오늘"  → [오늘]
"날씨가" → [오늘, 날씨가]
"좋다"  → [오늘, 날씨가, 좋다]
```

### GPT의 사전학습

- **다음 토큰 예측 (Next Token Prediction)**: 대규모 텍스트에서 다음 단어를 맞추는 것만으로 학습
- BERT의 MLM과 비교: BERT는 양방향으로 [MASK]를 맞추고, GPT는 왼쪽만 보고 다음을 예측
- 이 단순한 목표로 **문법, 사실, 추론 능력**까지 학습됨 (규모가 커지면서)

### GPT 시리즈의 발전

| 모델 | 파라미터 | 핵심 기여 | 학습 방식 |
|------|---------|----------|----------|
| **GPT-1** (2018) | 1.17억 | Decoder 사전학습 + 파인튜닝 패러다임 제시 | 사전학습 → **작업별 파인튜닝** |
| **GPT-2** (2019) | 15억 | 파인튜닝 없이도 다양한 작업 수행 가능성 제시 | 사전학습만으로 zero-shot 시도 |
| **GPT-3** (2020) | 1,750억 | **In-Context Learning** 능력 입증 | few-shot 프롬프트로 작업 수행 |

#### GPT-1: 파인튜닝 방식

```
사전학습: 대규모 텍스트로 다음 토큰 예측 학습
    ↓
파인튜닝: 각 작업(분류, QA 등)에 맞는 라벨 데이터로 추가 학습
    - 분류: [CLS] 토큰 위에 분류 헤드 추가
    - QA: 시작/끝 위치 예측 헤드 추가
```

- BERT와 비슷한 "사전학습 → 파인튜닝" 패러다임이지만, **Decoder 구조**를 사용

#### GPT-2: 파인튜닝 없는 가능성

- "Language Models are Unsupervised Multitask Learners"
- 충분히 큰 모델은 파인튜닝 없이도 다양한 작업을 수행할 수 있다는 것을 보여줌
- 하지만 성능이 파인튜닝 모델에 미치지 못함

#### GPT-3: In-Context Learning의 등장

- 파라미터를 1,750억으로 대폭 키움 → **창발적 능력** 발현
- 프롬프트에 예시를 넣으면 파인튜닝 없이 작업 수행 가능 (ICL)
- 모델 크기가 커질수록 ICL 성능이 급격히 향상

### BERT vs GPT 비교 정리

| | BERT | GPT |
|--|------|-----|
| 구조 | Encoder | Decoder |
| 방향 | 양방향 | 단방향 (왼→오) |
| 학습 목표 | 마스크된 단어 예측 (MLM) | 다음 단어 예측 |
| 강점 | 이해/분류 | 생성 |
| 사용법 | 파인튜닝 | 프롬프트 (In-Context Learning) |

---

## 2. In-Context Learning (ICL)

### 패러다임의 전환

3차시에서 BERT의 "사전학습 → 파인튜닝" 패러다임을 배웠다. GPT 계열은 여기서 한 단계 더 나아간다:

```
BERT 시대:  사전학습 → 파인튜닝 (작업별 라벨 데이터 필요)
GPT 시대:   사전학습 → 프롬프트만으로 수행 (파인튜닝 불필요!)
```

### In-Context Learning이란?

모델의 파라미터를 업데이트하지 않고, **프롬프트에 예시/지시를 포함**시켜 모델이 작업을 수행하게 하는 방법

### 세 가지 방식

#### Zero-shot

```
프롬프트: "다음 문장의 감정을 분류하세요: '이 영화 정말 재미있다'"
출력: "긍정"
```
- 예시 없이 **지시만** 제공
- 모델의 사전학습 지식에만 의존

#### Few-shot

```
프롬프트:
"문장: '최고의 영화!' → 감정: 긍정
 문장: '시간 낭비였다' → 감정: 부정
 문장: '이 영화 정말 재미있다' → 감정: "
출력: "긍정"
```
- 몇 개의 **입출력 예시**를 프롬프트에 포함
- 모델이 패턴을 파악하여 새 입력에 적용
- 파인튜닝 없이 소수의 예시만으로 작업 수행

#### One-shot

- Few-shot의 특수 경우: 예시 **1개**만 제공

### 왜 가능한가?

- LLM이 사전학습 과정에서 **방대한 패턴**을 학습
- 프롬프트의 예시가 모델에게 "이런 형식으로 답하라"는 **맥락**을 제공
- 모델 크기가 커질수록 ICL 능력이 급격히 향상 (창발적 능력)

### Chain of Thought (CoT) Prompting

ICL의 확장 기법. 단순히 답만 보여주는 것이 아니라, **풀이 과정(사고 과정)**을 함께 보여줘서 모델의 추론 능력을 끌어내는 방법.

#### 일반 Few-shot vs CoT

```
[일반 Few-shot]
Q: 식당에 23명이 있었다. 11명이 떠나고 6명이 왔다. 몇 명인가?
A: 18

[Chain of Thought]
Q: 식당에 23명이 있었다. 11명이 떠나고 6명이 왔다. 몇 명인가?
A: 처음에 23명이 있었다. 11명이 떠나면 23-11=12명.
   6명이 오면 12+6=18명. 답은 18명이다.
```

- 풀이 과정을 보여주면 모델이 **단계적 추론**을 수행하게 됨
- 특히 **수학, 논리, 상식 추론** 문제에서 성능 대폭 향상

#### Zero-shot CoT

예시 없이 **"Let's think step by step"** 한 마디만 추가해도 추론 성능 향상

```
Q: 식당에 23명이 있었다. 11명이 떠나고 6명이 왔다. 몇 명인가?
   Let's think step by step.
A: (모델이 알아서 단계적으로 풀이)
```

| 방식 | 프롬프트에 포함하는 것 | 강점 |
|------|---------------------|------|
| Zero-shot | 지시만 | 간단하지만 성능 제한 |
| Few-shot | 예시 (입력→출력) | 패턴 학습 |
| **CoT** | 예시 + **풀이 과정** | 복잡한 추론 가능 |
| **Zero-shot CoT** | "단계별로 생각하자" | 예시 없이도 추론 향상 |

### ICL vs 파인튜닝

| | In-Context Learning | 파인튜닝 |
|--|---------------------|---------|
| 파라미터 변경 | X (그대로) | O (업데이트) |
| 필요한 데이터 | 프롬프트에 들어갈 몇 개 | 수백~수만 개 라벨 데이터 |
| 장점 | 빠르고 유연 | 높은 정확도 |
| 단점 | 프롬프트 길이 제한, 불안정 | 시간/비용, 작업마다 별도 모델 |

---

## 3. 디코딩 알고리즘

Decoder 모델이 텍스트를 생성할 때, 다음 토큰을 **어떤 전략으로 선택하는가**의 문제. 모델은 각 토큰에 대한 확률 분포를 출력하고, 디코딩 알고리즘이 이 분포에서 토큰을 선택한다.

### Greedy Decoding

- 매 시점마다 **확률이 가장 높은 토큰 1개**만 선택
- 단순하고 빠르지만 **전체적으로 최적이 아닐 수 있음**

```
예: 각 시점의 확률
시점1: "나는"(0.6) "내가"(0.4) → "나는" 선택
시점2: "밥을"(0.7) "빵을"(0.3) → "밥을" 선택
→ "나는 밥을..." (각 단계 최선이지만 전체 문장은 최적 아닐 수 있음)
```

- **문제**: 한 번 선택하면 되돌릴 수 없음 → **지역 최적(local optimum)**에 빠짐
- 예: 시점1에서 "내가"를 골랐으면 더 자연스러운 문장이 나왔을 수도 있음

### Beam Search

- 매 단계마다 **상위 k개 후보 문장을 유지**하며 탐색 (k = beam width)
- 각 후보를 확장하고, 전체 중 확률 높은 상위 k개만 남김

```
k=2 예시:

1단계: <start>
2단계:
  "나는" (0.6)
  "내가" (0.4)  → 2개 유지

3단계: 각 후보 확장
  "나는 밥을" (0.6×0.7=0.42)
  "나는 빵을" (0.6×0.3=0.18)
  "내가 너를" (0.4×0.6=0.24)
  "내가 밥을" (0.4×0.4=0.16)
  → 상위 2개: "나는 밥을"(0.42), "내가 너를"(0.24) 유지

4단계: 계속 반복 → 최종 최고 확률 문장 선택
```

| Greedy | Beam Search |
|--------|-------------|
| 1개만 선택 | k개 후보 유지 |
| 빠름, 결정적 | k 커질수록 계산량 증가 |
| 비최적 가능 | 더 나은 결과, 항상 최적은 아님 |
| 다양성 없음 | 여전히 다양성 부족 |

### Sampling 기반 디코딩

확률 분포에서 **무작위로 샘플링**하여 다양한 텍스트 생성. 창의적/대화적 생성에 적합.

#### Temperature Sampling

확률 분포의 **날카로움/평탄함**을 조절하는 파라미터

```
P(wᵢ) = exp(zᵢ / T) / Σ exp(zⱼ / T)

T: Temperature (온도)
zᵢ: 모델이 출력한 로짓(logit) 값
```

| T 값 | 효과 | 비유 |
|------|------|------|
| **T → 0** | 확률 최고 토큰만 선택 (≈ Greedy) | 가장 안전한 답만 |
| **T < 1** | 분포가 날카로워짐 → 보수적 | 확실한 단어 위주 |
| **T = 1** | 원래 확률 분포 그대로 | 모델 본래 성향 |
| **T > 1** | 분포가 평탄해짐 → 창의적 | 다양한 단어 시도 |

```
예: 로짓이 [2.0, 1.0, 0.5]인 경우

T=0.5 (보수적): [0.76, 0.17, 0.07] → 첫 번째 토큰 높은 확률
T=1.0 (기본):   [0.51, 0.28, 0.21] → 골고루
T=2.0 (창의적): [0.39, 0.33, 0.28] → 거의 균등
```

#### Top-k Sampling

- 확률 상위 **k개 토큰**만 후보로 남기고 나머지 제외
- k개 중에서 확률에 비례하여 샘플링
- k가 작으면 → 보수적, k가 크면 → 다양

```
전체 확률: 먹다(0.4), 마시다(0.25), 보다(0.15), 듣다(0.1), 날다(0.05), ...

Top-3: 먹다(0.4), 마시다(0.25), 보다(0.15) → 정규화 후 샘플링
나머지 토큰은 후보에서 제외
```

#### Top-p (Nucleus) Sampling

- 확률을 높은 순서대로 누적하여, 누적 확률이 **p를 넘는 지점**까지의 토큰만 후보
- Top-k와 달리 후보 수가 **상황에 따라 유동적**

```
예: p=0.9
확률 순서: 먹다(0.5), 마시다(0.3), 보다(0.15), 듣다(0.05)
누적:      0.5       0.8        0.95       1.0
→ 0.9를 넘는 지점까지: "먹다, 마시다, 보다" 3개가 후보

다른 상황에서:
확률 순서: 사랑(0.85), 좋아(0.10), 싫어(0.05)
누적:      0.85       0.95       1.0
→ "사랑, 좋아" 2개만 후보 (확신이 높으면 후보가 적어짐)
```

### 디코딩 전략 비교 요약

| 전략 | 특징 | 결과 다양성 | 결정적? | 주 사용처 |
|------|------|-----------|---------|----------|
| **Greedy** | 확률 최고 1개 | 낮음 | 결정적 | 빠른 추론 |
| **Beam Search** | k개 후보 유지 | 낮음 | 결정적 | 번역, 요약 |
| **Temperature** | 온도로 분포 조절 | 조절 가능 | 확률적 | 대화, 창작 |
| **Top-k** | 상위 k개에서 샘플 | 중간 | 확률적 | 범용 |
| **Top-p** | 누적확률 p까지 샘플 | 높음 (유동적) | 확률적 | 대화, 창작 |

> 실전에서는 **Temperature + Top-p**를 조합하여 사용하는 경우가 많다.

---

## 4. 거대 언어 모델 (LLM)의 평가

### LLM이란?

- 대규모 텍스트 데이터로 사전학습된 초대형 Decoder 언어 모델
- 수십억~수천억 개의 파라미터 (GPT-3: 1750억, GPT-4: 추정 수조)
- 텍스트 생성, 질의응답, 추론, 코딩 등 범용적 능력

### 평가가 어려운 이유

1. 정답이 하나가 아닌 **개방형 생성** 과제가 많음
2. 같은 질문에도 다양한 좋은 답이 존재
3. 사실성, 유용성, 안전성 등 **다차원적 품질** 평가 필요
4. 특정 벤치마크에 과적합될 수 있음 (데이터 오염)

### 자동 평가 지표

| 지표 | 설명 | 평가 대상 | 기준 |
|------|------|----------|------|
| **Perplexity** | 다음 토큰 예측 혼란도 | 언어 모델 자체 품질 | 낮을수록 좋음 |
| **BLEU** | N-gram 정밀도 겹침 | 번역 | 1에 가까울수록 |
| **ROUGE** | N-gram 재현율 겹침 | 요약 | 1에 가까울수록 |
| **MMLU** | 57개 과목 객관식 문제 | 지식/추론 능력 | 높을수록 |
| **HumanEval** | 코딩 문제 테스트 통과율 | 코드 생성 | 높을수록 |
| **GSM8K** | 초등 수학 문제 | 수학적 추론 | 높을수록 |
| **TruthfulQA** | 사실성 검증 질문 | 환각(hallucination) 정도 | 높을수록 |

### 인간 평가

- 자동 지표만으로는 **유용성, 자연스러움, 안전성**을 온전히 측정 어려움
- 사람이 직접 모델 출력을 평가하거나 **쌍대 비교** (A vs B 중 더 좋은 답 선택)
- 비용이 높고 시간이 오래 걸림

### LLM-as-a-Judge

- 강력한 LLM(예: GPT-4)을 **평가자로 사용**
- 사람 평가의 비용 문제를 완화하면서 비슷한 품질의 평가
- 주의: 평가 LLM 자체의 편향 (자기 출력을 선호, 긴 답변 선호 등)

### 벤치마크와 리더보드

| 벤치마크 | 특징 |
|---------|------|
| **Chatbot Arena** | 사용자가 두 모델 응답을 비교 → Elo 레이팅 |
| **Open LLM Leaderboard** | 여러 벤치마크 점수 종합 순위표 |
| **MMLU / HellaSwag / ARC** | 지식, 상식, 과학 추론 등 다양한 능력 측정 |

---

## 5. 선호 학습 (Preference Learning)

### 왜 필요한가?

사전학습(다음 토큰 예측)과 SFT(지도 미세조정)만으로는 부족:

| 문제 | 설명 |
|------|------|
| **환각 (Hallucination)** | 사실이 아닌 내용을 그럴듯하게 생성 |
| **유해한 출력** | 폭력적, 차별적, 불법적 내용 생성 |
| **쓸데없이 긴 답변** | 질문에 대해 불필요하게 장황한 응답 |
| **지시 미이행** | 사용자의 의도와 다른 방향으로 답변 |

→ 모델을 사람이 원하는 답변 스타일로 **정렬(Alignment)** 해야 함

### LLM 학습의 3단계

```
1단계: 사전학습 (Pre-training)
  - 대규모 텍스트로 "다음 토큰 예측" 학습
  - 언어 지식, 사실, 추론 능력 습득
  - 아직 지시를 따르지 못함

2단계: SFT (Supervised Fine-Tuning)
  - (지시, 양질의 응답) 쌍으로 지도학습
  - 지시를 따르는 법을 배움
  - 여전히 유해/부정확한 답 가능

3단계: 선호 학습 (Alignment)
  - 사람의 선호에 맞게 미세 조정
  - RLHF 또는 DPO
  - 유용하고 안전한 모델 완성
```

### RLHF (Reinforcement Learning from Human Feedback)

사람의 선호를 반영하여 모델을 정렬하는 대표적 방법

#### 3단계 프로세스

| 단계 | 내용 | 상세 |
|------|------|------|
| **1. SFT** | 양질의 데이터로 지도학습 | (지시, 좋은 응답) 쌍으로 파인튜닝 |
| **2. 보상 모델 (RM)** | 사람 선호 데이터로 보상 모델 학습 | 같은 질문에 대한 두 응답 중 어느 것이 나은지 사람이 판단 → 점수화 |
| **3. RL 최적화 (PPO)** | 보상 점수를 최대화하도록 강화학습 | LM이 생성 → RM이 점수 부여 → 높은 점수를 받도록 LM 업데이트 |

#### 보상 모델 학습 (2단계 상세)

```
질문: "파이썬이란?"
응답 A: "파이썬은 1991년 귀도 반 로섬이 만든 범용 프로그래밍 언어로..."
응답 B: "파이썬은 뱀의 한 종류입니다."

사람 평가: A > B (A가 더 유용한 답변)

→ 이런 (질문, A > B) 쌍대 비교 데이터를 수천~수만 개 수집
→ 보상 모델이 "좋은 응답 = 높은 점수, 나쁜 응답 = 낮은 점수"를 예측하도록 학습
```

#### PPO 최적화 (3단계 상세)

```
반복:
  1. LM이 질문에 대한 응답 생성
  2. 보상 모델이 응답에 점수 부여
  3. 점수가 높아지는 방향으로 LM 파라미터 업데이트 (강화학습)
  4. 단, 원래 SFT 모델에서 너무 멀어지지 않도록 KL 페널티 부여
```

- **KL 페널티**: 보상만 최대화하면 보상 모델의 허점을 악용(reward hacking)할 수 있으므로, 원래 모델과 비슷하게 유지하는 제약

### DPO (Direct Preference Optimization)

- RLHF의 **간소화 버전**
- 보상 모델을 별도로 학습하지 않고, 선호 데이터로 **직접** 언어 모델을 최적화
- 수학적으로 RLHF와 동일한 최적해를 찾지만, 구현이 훨씬 간단

| | RLHF | DPO |
|--|------|-----|
| 보상 모델 | 별도로 학습 필요 | **불필요** |
| 강화학습 (PPO) | 필요 (학습 불안정 가능) | **불필요** |
| 구현 복잡도 | 높음 | 낮음 (일반 분류처럼 학습) |
| 학습 안정성 | PPO 하이퍼파라미터에 민감 | 안정적 |
| 성능 | 좋음 | RLHF와 비슷하거나 나은 경우도 |

### DPO의 직관

```
DPO 손실 함수의 핵심:

"선호 응답(A)의 확률을 높이고, 비선호 응답(B)의 확률을 낮춘다"

별도 보상 모델 없이, (질문, A > B) 데이터만으로 직접 LM을 업데이트
```

### 선호 학습의 큰 그림

> 사전학습(지식 습득) → SFT(지시 따르기) → **선호 학습(정렬)** = 현대 LLM의 완성 단계
>
> 선호 학습은 단순히 "더 좋은 답"을 만드는 것이 아니라, 모델을 **안전하고 유용하게** 만드는 핵심 기술이다. ChatGPT의 성공 비결 중 하나가 바로 이 RLHF 과정이다.

---

## 차시 연결 요약

> 4차시는 3차시의 Transformer에서 이어지는 **생성 모델(Decoder)의 세계**다:
>
> - **Decoder 모델**(GPT): Transformer Decoder만 사용, 자기회귀적 생성
> - **In-Context Learning**: 파인튜닝 없이 프롬프트만으로 작업 수행 (BERT의 패러다임을 넘어섬)
> - **디코딩 알고리즘**: 생성 시 토큰 선택 전략 (Greedy → Beam → Sampling)
> - **LLM 평가**: 3차시의 Perplexity/BLEU에서 확장된 다차원 평가
> - **선호 학습**: 사전학습 → SFT → RLHF/DPO로 이어지는 LLM 완성 파이프라인
>
> 5차시에서는 텍스트가 아닌 **이미지**를 다루는 CNN으로 넘어가고, 6차시에서 텍스트+이미지를 합친 **멀티모달 모델**로 통합된다.
