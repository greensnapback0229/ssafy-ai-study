# AI & 기계학습 핵심 공식 & 개념 치트시트

## 1차시: ML 기초 & 평가 지표

### 오류 측정 공식
| 수식 | 의미 |
|------|------|
| **MSE** = (1/n) Σ(yᵢ - ŷᵢ)² | 예측과 실제값 차이의 제곱 평균 |
| **RMSE** = √MSE | MSE에 루트 (원래 단위) |
| **R²** = 1 - (RSS/TSS) | 설명력 비율 (0~1) |
| **RSS** = Σ(yᵢ - ŷᵢ)² | 잔차제곱합 (모델이 설명 못한 변동) |
| **TSS** = Σ(yᵢ - ȳ)² | 총제곱합 (데이터 총 변동) |

### 혼동행렬 지표
| 지표 | 수식 | 의미 |
|------|------|------|
| **정확도** | (TP+TN)/전체 | 전체 중 맞춘 비율 |
| **정밀도** | TP/(TP+FP) | 양성 예측 중 실제 양성 |
| **재현율** | TP/(TP+FN) | 실제 양성 중 맞춘 비율 |
| **F1** | 2PR/(P+R) | 정밀도와 재현율 조화평균 |

---

## 2차시: 회귀, 분류, 신경망

### 선형회귀
| 개념 | 수식 |
|------|------|
| **단순선형회귀** | Y = b₀ + b₁X + e |
| **최적 기울기** | b₁ = Σ(xᵢ-x̄)(yᵢ-ȳ) / Σ(xᵢ-x̄)² |
| **최적 절편** | b₀ = ȳ - b₁x̄ |

### 로지스틱 회귀
| 개념 | 수식 |
|------|------|
| **시그모이드** | σ(z) = 1 / (1 + e⁻ᶻ) |
| **로지스틱 모형** | P(Y=1\|X) = σ(b₀ + b₁X) |
| **오즈** | Odds = P/(1-P) |

### 경사하강법
| 개념 | 수식 |
|------|------|
| **파라미터 업데이트** | w = w - η × (dL/dw) |
| η: 학습률, dL/dw: 손실함수의 기울기 | 기울기 반대 방향으로 이동 |

### 신경망
| 구조 | 수식 |
|------|------|
| **한 층 연산** | y = σ(Wx + b) |
| **ReLU** | max(0, x) |
| **Tanh** | tanh(x) ∈ [-1, 1] |

---

## 3차시: 자연어처리 기본

### 텍스트 표현
| 방식 | 특징 |
|------|------|
| **원핫 인코딩** | 한 원소만 1, 나머지 0. 희소(sparse) |
| **워드 임베딩** | 저차원 밀집(dense) 벡터. 의미적 관계 포착 |

### RNN
| 개념 | 수식 |
|------|------|
| **Hidden State** | hₜ = f(W_h·hₜ₋₁ + W_x·xₜ + b) |
| **기울기 소실** | 역전파 시 W_h가 반복 곱해져 기울기 → 0 |

### LSTM (게이트 3개)
| 게이트 | 수식 | 역할 |
|--------|------|------|
| **Forget** | fₜ = σ(W_f·[hₜ₋₁, xₜ] + b_f) | 이전 기억 중 버릴 것 (0→삭제, 1→유지) |
| **Input** | iₜ = σ(W_i·[hₜ₋₁, xₜ] + b_i) | 새 정보 중 저장할 것 |
| **Output** | oₜ = σ(W_o·[hₜ₋₁, xₜ] + b_o) | Cell에서 출력할 것 |
| **Cell Update** | Cₜ = fₜ⊙Cₜ₋₁ + iₜ⊙C̃ₜ | 기억 업데이트 (덧셈=기울기 보존) |
| **Hidden Output** | hₜ = oₜ⊙tanh(Cₜ) | 현재 출력 |

### 언어 모델 평가
| 지표 | 의미 |
|------|------|
| **Perplexity** | 2^(-1/N·Σlog₂P(wᵢ)) — 낮을수록 좋음 |
| **BLEU** | N-gram 정밀도 겹침 (번역 평가) |
| **ROUGE** | N-gram 재현율 겹침 (요약 평가) |

### Transformer (Self-Attention)
| 개념 | 수식 |
|------|------|
| **Query, Key, Value** | Q, K, V로부터 3개 벡터 생성 |
| **Attention Score** | score = Q·K^T |
| **Attention 정규화** | softmax(score / √d_k) |
| **Context 벡터** | weights × V |

### BERT (마스킹 학습)
| 방식 | 설명 |
|------|------|
| **MLM** | 마스킹된 단어를 양방향 문맥으로 예측 |
| **NSP** | 두 문장이 이어지는지 이진 분류 |

---

## 4차시: LLM, 디코딩, 선호학습

### Decoder 모델 (GPT)
| 특징 | 설명 |
|------|------|
| **자기회귀 생성** | 이전 토큰 → 다음 토큰 1개 예측 반복 |
| **Masked Self-Attention** | 미래 토큰 차단, 현재까지만 참고 |
| **다음 토큰 예측** | 사전학습 목표 (Next Token Prediction) |

### In-Context Learning (ICL)
| 방식 | 설명 |
|------|------|
| **Zero-shot** | 지시만 제공 |
| **Few-shot** | 예시(입출력 쌍) 포함 |
| **Chain of Thought** | 풀이 과정 포함 → 추론 성능 향상 |

### 디코딩 알고리즘
| 방식 | 특징 |
|------|------|
| **Greedy** | 확률 최고 1개만 선택 (빠르지만 비최적) |
| **Beam Search** | 상위 k개 후보 유지 → 더 나은 결과 |
| **Temperature** | P(wᵢ) = exp(zᵢ/T) / Σexp(zⱼ/T) |
| **Top-k** | 상위 k개 토큰에서 샘플링 |
| **Top-p (Nucleus)** | 누적확률 p까지의 토큰만 샘플링 |

### 선호 학습 (RLHF)
| 단계 | 내용 |
|------|------|
| **1. SFT** | 양질의 데이터로 지도학습 |
| **2. 보상 모델** | 사람 선호로부터 보상 함수 학습 |
| **3. PPO** | 보상 최대화하도록 강화학습 (KL 페널티 적용) |

### DPO (Direct Preference Optimization)
| 특징 | 설명 |
|------|------|
| 손실 함수 | "선호 응답의 확률↑, 비선호 응답의 확률↓" |
| 장점 | RLHF보다 간단, 보상 모델 불필요 |

---

## 5차시: CNN (합성곱 신경망)

### CNN 핵심 원리
| 원리 | 설명 |
|------|------|
| **지역적 연결** | 작은 영역(receptive field)만 연결 |
| **가중치 공유** | 같은 필터를 전체에 재사용 |
| **계층적 학습** | 저수준(엣지) → 고수준(객체) 특징 |

### 합성곱 출력 크기 ★★★
```
출력 크기 = (W - F + 2P) / S + 1

W: 입력 크기
F: 필터 크기
P: 패딩
S: 스트라이드
```

### 패딩 (Padding)
| 종류 | 설명 | 출력 크기 |
|------|------|----------|
| **Valid** | 패딩 없음 | 줄어듦 |
| **Same** | 0 추가 → 입력과 같은 크기 | 같음 |

### Pooling
| 종류 | 설명 |
|------|------|
| **Max Pooling** | 영역 내 최댓값 → 뚜렷한 특징 보존 |
| **Average Pooling** | 영역 내 평균 → 전반적 수준 보존 |
| **Global Avg Pool** | 전체 특징맵 → 1개 값 |

### CNN 아키텍처 비교
| 모델 | 핵심 특징 |
|------|----------|
| **AlexNet** (2012) | 딥러닝 부흥의 시작 |
| **VGGNet** (2014) | 3×3 필터만 깊게 쌓기 |
| **ResNet** (2015) | Skip Connection (Cₜ = fₜ⊙Cₜ₋₁ + iₜ⊙C̃ₜ) 기울기 보존 |
| **MobileNet** (2017) | Depthwise Separable Conv — 경량화 |

---

## 6차시: 멀티모달, 이미지 생성, 파운데이션 모델

### ViT (Vision Transformer)
| 특징 | 설명 |
|------|------|
| **패치 분할** | 이미지 → 16×16 패치 → 토큰처럼 처리 |
| **Self-Attention** | 모든 패치를 동시에 참조 (병렬 처리) |
| 장점 | 대규모 데이터에서 CNN 능가 |

### CLIP (Contrastive Language-Image Pre-training)
| 구성 | 역할 |
|------|------|
| **Image Encoder** | 이미지 → 벡터 (ViT/ResNet) |
| **Text Encoder** | 텍스트 → 벡터 (Transformer) |
| **대조 학습** | 맞는 쌍: 유사도 높게, 틀린 쌍: 낮게 |

### CLIP vs SigLIP
| | CLIP (Softmax) | SigLIP (Sigmoid) |
|--|----------------|-----------------|
| 비교 방식 | 배치 내 상대적 비교 | 각 쌍 독립적 판단 |
| 배치 크기 | 크게 필요 (32K) | 덜 민감 |
| 계산 | 정규화 필요 (비쌈) | 이진 분류 (효율적) |

### 이미지 생성 모델
| 모델 | 원리 | 품질 | 속도 | 안정성 |
|------|------|------|------|--------|
| **GAN** | 생성자-판별자 경쟁 | 높음 | 빠름 | 불안정 |
| **VAE** | 잠재 공간 학습 + 복원 | 중간 | 빠름 | 안정적 |
| **Diffusion** | 노이즈 추가/제거 학습 | **매우 높음** | 느림 | **안정적** |

### Diffusion Model
| 과정 | 설명 |
|------|------|
| **Forward** | 원본 → +노이즈 반복 → 순수 노이즈 |
| **학습 목표** | 각 단계에서 **추가된 노이즈 예측** |
| **Reverse** | 노이즈 → -노이즈 반복 → 깨끗한 이미지 |

### 파운데이션 모델 특징
| 특징 | 설명 |
|------|------|
| **규모의 법칙** | 크기 증가 → 예측 가능한 성능 향상 |
| **창발적 능력** | 임계값 넘으면 갑자기 새 능력 출현 (예: few-shot) |
| **전이 학습** | 사전학습 → 새 작업에 파인튜닝 |

### 활성함수 비교
| 함수 | 수식 | 출력 | 특징 |
|------|------|------|------|
| Sigmoid | 1/(1+e⁻ˣ) | 0~1 | 기울기 소실 |
| ReLU | max(0, x) | 0~∞ | **표준**. 죽은 뉴런 문제 |
| Leaky ReLU | x>0? x : αx | -∞~∞ | ReLU 개선 (음수도 기울기 있음) |
| GELU | x·Φ(x) | -∞~∞ | Transformer 표준 |

### 가중치 초기화
| 방법 | 적합한 활성함수 | 분산 |
|------|-----------------|------|
| **Xavier** | Sigmoid, Tanh | 2/(nᵢₙ + nₒᵤₜ) |
| **He** | ReLU | 2/nᵢₙ |

---

## 빠른 참조: 주요 모델과 차시 연결

| 모델 | 차시 | 주요 수식/개념 |
|------|------|---------------|
| MSE/R² | 1차 | R² = 1 - RSS/TSS |
| 혼동행렬 | 1차 | F1 = 2PR/(P+R) |
| 선형회귀 | 2차 | Y = b₀ + b₁X |
| 시그모이드 | 2차 | σ(z) = 1/(1+e⁻ᶻ) |
| 경사하강법 | 2차 | w = w - η·∇L |
| RNN | 3차 | hₜ = f(W_h·hₜ₋₁ + W_x·xₜ + b) |
| LSTM 게이트 | 3차 | Cₜ = fₜ⊙Cₜ₋₁ + iₜ⊙C̃ₜ |
| Self-Attention | 3차 | softmax(QK^T/√dₖ)V |
| Beam Search | 4차 | k개 후보 유지하는 탐색 |
| Temperature | 4차 | P(w) = exp(z/T)/Σexp(z/T) |
| CNN 출력크기 | 5차 | (W-F+2P)/S + 1 |
| Pooling | 5차 | Max 또는 Average |
| ViT | 6차 | 이미지 패치 → Transformer |
| CLIP 대조학습 | 6차 | 맞는 쌍↑, 틀린 쌍↓ |
| Diffusion | 6차 | Forward + Reverse 노이즈 과정 |

---

**핵심 정리**
- **1차**: 평가 지표 (MSE, R², 혼동행렬)
- **2차**: 선형/로지스틱 회귀 + 경사하강법 + 신경망 기초
- **3차**: RNN/LSTM + Transformer + BERT (양방향 이해)
- **4차**: GPT/LLM + In-Context Learning + 디코딩 + 선호학습
- **5차**: CNN 구조 + 합성곱 + 풀링 + 아키텍처
- **6차**: ViT + CLIP + Diffusion + 파운데이션 모델
