# AI & 기계학습 예상 시험 문제

## 주관식 단답 (15문제)

### 1차시 - AI 기초 개념

**1. AI, ML, DL의 포함 관계를 설명하고, 각각의 핵심 차이점을 한 문장으로 기술하시오.**

답: AI는 인간의 지능을 모방하는 기술의 총칭이고, ML은 명시적 프로그래밍 없이 데이터에서 패턴을 학습하는 AI의 하위 분야이며, DL은 다층 신경망으로 특징을 자동 추출하는 ML의 하위 분야이다.

**2. 혼동행렬에서 정밀도(Precision)와 재현율(Recall)이 높은 것이 모두 중요한 이유를 의료 진단 예시를 들어 설명하시오.**

답: 정밀도가 낮으면 정상인을 환자로 잘못 진단하여 불필요한 치료를 받고, 재현율이 낮으면 실제 환자를 정상으로 판정하여 치료를 받지 못해 생명에 위험을 초래하기 때문이다.

**3. R²(결정계수)가 음수(R² < 0)인 경우는 언제이며, 이것이 의미하는 바는 무엇인가?**

답: 모델의 예측이 모든 데이터에 평균값을 대입하는 것보다 더 나쁠 때 발생하며, 이는 모델이 오히려 해로워서 사용하면 안 된다는 의미이다.

**4. k-means 알고리즘에서 초기 클러스터 중심을 어떻게 선택하느냐가 중요한 이유를 기술하시오.**

답: k-means는 항상 전역 최적해를 보장하지 않으므로, 초기값에 따라 수렴하는 지점이 달라질 수 있기 때문이다.

---

### 2차시 - 회귀, 분류, 신경망

**5. 선형회귀에서 최소제곱법이 최소화하는 것이 무엇이며, 이를 1차시의 R²와 어떻게 연결되는지 설명하시오.**

답: 최소제곱법은 잔차제곱합(RSS)을 최소화하며, RSS를 줄이면 R² = 1 - RSS/TSS 공식에 의해 R²가 올라가므로 좋은 회귀 모델이 된다.

**6. 로지스틱 회귀에서 시그모이드 함수 σ(z) = 1/(1+e⁻ᶻ)를 사용하는 이유는 무엇인가?**

답: 선형회귀의 출력은 -∞~+∞ 범위인데, 분류 문제의 확률은 반드시 0~1 범위에 있어야 하므로 시그모이드 함수로 출력을 0~1로 압축하기 위해서이다.

**7. 신경망에서 활성함수(ReLU, Sigmoid 등)가 필수인 이유를 활성함수 없이 여러 층을 쌓은 경우를 예시로 들어 설명하시오.**

답: 활성함수 없이 y = W₂(W₁x + b₁) + b₂ = W'x + b'처럼 여러 층을 쌓아도 결과적으로 하나의 선형 변환이 되므로, 층을 쌓는 의미가 없기 때문이다. 활성함수가 비선형성을 추가하여 복잡한 패턴을 학습할 수 있게 한다.

**8. 경사하강법에서 학습률(η)이 너무 크거나 너무 작으면 각각 어떤 문제가 발생하는가?**

답: η가 너무 크면 최적점을 지나쳐 발산하고, η가 너무 작으면 수렴이 매우 느려져 학습에 시간이 오래 걸린다.

---

### 3차시 - 자연어처리

**9. 원핫 인코딩과 워드 임베딩의 가장 큰 차이점 두 가지를 기술하시오.**

답: 첫째, 원핫은 차원수가 사전 크기(수만~수십만)인 반면 임베딩은 저차원(예: 300)이다. 둘째, 원핫에서는 모든 단어 쌍의 유사도가 0(직교)이지만, 임베딩에서는 의미적으로 비슷한 단어들이 벡터 공간에서 가까이 위치한다.

**10. RNN의 기울기 소실 문제가 발생하는 원인을 역전파 과정에서 W_h가 반복 곱해지는 것과 연결하여 설명하시오.**

답: RNN의 역전파에서 기울기가 ∂L/∂h₁ = ... × W_h × W_h × W_h처럼 W_h가 여러 번 곱해지는데, W_h의 값이 1보다 작으면 반복 곱셈에 의해 기울기가 기하급수적으로 작아져 0에 수렴하게 된다.

**11. LSTM의 Cell State와 Hidden State가 각각 하는 역할을 비유를 사용하여 설명하시오.**

답: Cell State는 "노트에 적어둔 장기 기억"처럼 정보가 거의 변하지 않고 먼 거리를 흐르는 고속도로이고, Hidden State는 "지금 머릿속에 떠올린 단기 기억"으로 현재 시점의 출력을 담당한다.

**12. Transformer의 Self-Attention이 RNN보다 장기 의존성을 잘 학습하는 이유를 기술하시오.**

답: Self-Attention은 모든 단어를 동시에 참조할 수 있어 먼 단어 간의 거리가 1로 계산되므로 기울기가 잘 전달되고, RNN처럼 곱셈 연쇄가 없어서 기울기 소실이 없기 때문이다.

**13. BERT의 MLM(Masked Language Model) 학습과 GPT의 다음 토큰 예측 학습의 근본적 차이점을 기술하시오.**

답: BERT는 [MASK]된 단어 주변의 왼쪽 AND 오른쪽 문맥을 모두 보고 예측하는 양방향 학습이고, GPT는 왼쪽 문맥만 보고 다음 단어를 예측하는 단방향(왼→오) 학습이다.

---

### 4차시 - LLM, 디코딩, 선호 학습

**14. In-Context Learning(ICL)이 파인튜닝과 다른 점을 파라미터 업데이트 관점에서 설명하시오.**

답: ICL은 모델의 파라미터를 변경하지 않고 프롬프트에 예시/지시를 포함시켜 모델이 작업을 수행하게 하는 방식이고, 파인튜닝은 작업별 라벨 데이터로 파라미터를 업데이트하는 방식이다.

**15. Beam Search가 Greedy Decoding보다 더 나은 문장을 생성할 수 있는 이유를 설명하시오.**

답: Greedy Decoding은 매 시점마다 확률 최고인 토큰 1개만 선택하여 지역 최적에 빠질 수 있지만, Beam Search는 상위 k개 후보를 유지하며 탐색하므로 더 높은 확률의 전체 문장을 찾을 수 있기 때문이다.

---

### 5차시 - CNN

**16. CNN이 MLP 대비 이미지 처리에 훨씬 효율적인 이유를 가중치 공유 관점에서 설명하시오.**

답: CNN은 작은 필터(예: 3×3=9개 파라미터)를 이미지 전체에 재사용하지만, MLP는 모든 픽셀이 모든 뉴런에 연결되어 수백만 개 이상의 파라미터가 필요하기 때문이다.

**17. CNN의 합성곱 연산에서 출력 크기 공식 = (W - F + 2P) / S + 1에서 각 변수가 의미하는 바를 설명하시오.**

답: W는 입력 크기, F는 필터 크기, P는 패딩(경계 0 추가), S는 스트라이드(필터 이동 칸 수)이다.

**18. Max Pooling이 과적합을 방지하는 메커니즘을 설명하시오.**

답: Max Pooling으로 특징 맵의 공간 크기를 줄이면, 이후 FC 층에 들어가는 입력 차원이 감소하므로 모델의 전체 파라미터도 줄어들어 과적합이 완화된다.

---

### 6차시 - Vision-Language Models, 이미지 생성

**19. CLIP 모델에서 Zero-shot 분류가 가능한 이유를 임베딩 공간과 연결하여 설명하시오.**

답: CLIP은 이미지와 텍스트를 같은 공유 임베딩 공간에 임베딩하므로, 학습 시 본 적 없는 클래스라도 "a photo of X"라는 텍스트를 임베딩하고 이미지와의 유사도를 계산하여 분류할 수 있다.

**20. 현재 이미지 생성 모델로 GAN보다 Diffusion 모델을 더 선호하는 이유를 두 가지 이상 기술하시오.**

답: Diffusion은 GAN보다 학습이 안정적이고, 고화질 이미지를 생성하며, 텍스트 조건부 생성(text-to-image)에 매우 잘 적합하고, 다양한 이미지를 생성할 수 있기 때문이다.

---

## 객관식 (15문제)

### 1차시

**21. 다음 중 MSE(평균제곱오차)를 계산할 때 오차를 제곱하는 가장 중요한 이유는?**

① 계산 속도를 높이기 위해
② 양수/음수 오차 상쇄 방지 및 큰 오차에 더 큰 페널티 부여
③ 모델의 복잡도를 줄이기 위해
④ 확률 분포를 정규분포로 만들기 위해

정답: ② (양수/음수 오차가 상쇄되는 것을 방지하고, 제곱으로 인해 큰 오차에 더 큰 페널티를 부여한다)

**22. 훈련 데이터로 모델 성능을 측정하지 않고 별도의 테스트 데이터를 사용하는 이유는?**

① 모델의 일반화 능력을 정확히 평가하기 위해
② 훈련 데이터가 부족해서
③ 계산량을 줄이기 위해
④ 데이터의 다양성을 높이기 위해

정답: ① (훈련 데이터로 성능을 측정하면 과적합된 모델도 잘해 보이므로, 학습에 사용하지 않은 데이터로 평가해야 실제 일반화 성능을 알 수 있다)

**23. 클래스가 심하게 불균형인 데이터(양성 1%, 음성 99%)에서 정확도보다 중요한 지표는?**

① MSE
② F1 Score
③ R²
④ RMSE

정답: ② (불균형 데이터에서는 정밀도와 재현율의 조화평균인 F1 Score가 전부 음성이라고 예측한 모델도 정확도 99%가 되는 정확도의 함정을 피할 수 있다)

**24. k-means 클러스터링과 계층적 군집의 가장 큰 차이는?**

① 계산 속도
② 클러스터 개수를 사전에 정해야 하는지 여부
③ 사용하는 거리 함수
④ 필요한 데이터 크기

정답: ② (k-means는 k를 사전에 정해야 하지만, 계층적 군집은 덴드로그램에서 원하는 높이에서 잘라 결정할 수 있다)

---

### 2차시

**25. 로지스틱 회귀에서 출력값이 0.5보다 크면 양성으로, 작으면 음성으로 분류하는 근거는?**

① 시그모이드 함수의 중앙값이 0.5이기 때문
② 데이터 균형의 이유로
③ 오즈 비율 때문에
④ 수학적으로 증명되었기 때문

정답: ① (시그모이드 σ(z) = 1/(1+e⁻ᶻ)에서 z=0일 때 σ(0)=0.5이므로, 0.5가 자연스러운 경계가 된다)

**26. 경사하강법의 기본 업데이트 식 w = w - η × (dL/dw)에서 음수 부호(-)를 사용하는 이유는?**

① 계산 안정성을 위해
② 기울기의 반대 방향으로 이동하여 손실을 줄이기 위해
③ 데이터 정규화를 위해
④ 수렴 속도를 높이기 위해

정답: ② (손실함수의 기울기가 가리키는 방향은 손실이 증가하는 방향이므로, 그 반대 방향(-기울기)으로 이동해야 손실이 감소한다)

**27. 신경망에서 역전파(Backpropagation)의 주요 목적은?**

① 입력 데이터를 정규화하는 것
② 각 층의 가중치 기울기를 효율적으로 계산하는 것
③ 모델의 복잡도를 줄이는 것
④ 활성함수를 선택하는 것

정답: ② (역전파는 체인 룰을 사용하여 출력층에서부터 입력층 방향으로 각 가중치의 기울기를 효율적으로 계산한다)

**28. Shallow Network(얕은 신경망)와 Deep Network(깊은 신경망)를 구분하는 기준은?**

① 은닉층이 1개인지 2개 이상인지
② 파라미터 개수의 많고 적음
③ 필터 크기
④ 학습 데이터의 크기

정답: ① (얕은 신경망은 은닉층 1개, 깊은 신경망은 은닉층 2개 이상을 기준으로 구분한다)

---

### 3차시

**29. Word2Vec의 Skip-Gram 방식이 CBOW보다 유리한 상황은?**

① 데이터가 매우 많을 때
② 학습 속도가 중요할 때
③ 희귀 단어의 표현을 잘 학습해야 할 때
④ 계산량을 최소화해야 할 때

정답: ③ (Skip-Gram은 중심 단어가 주변 단어들을 예측하므로, 희귀 단어도 충분히 여러 번의 학습 신호를 받아 잘 학습된다)

**30. Perplexity(혼란도)로 측정하는 언어 모델의 성능에서, PPL=10의 의미는?**

① 10개의 단어만 예측할 수 있다는 뜻
② 모델이 정확히 10번 실패한다는 뜻
③ 모델이 각 단어마다 평균적으로 10개의 후보 중에서 선택하는 수준
④ 10개의 문장만 처리할 수 있다는 뜻

정답: ③ (PPL은 "다음 단어 예측 시 평균적으로 몇 개의 후보 중에서 고민하는가"를 나타내므로, PPL=10은 10개 후보 중 선택하는 수준을 의미한다)

**31. BERT의 MLM 사전학습에서 15%의 토큰을 [MASK]로 가리는 이유는?**

① [MASK] 토큰이 딱 15% 있어야 안정적이어서
② 양방향 문맥을 학습하기 위한 최적의 비율이기 때문
③ 계산량과 학습 효과의 균형을 맞추기 위해
④ BERT 아키텍처가 15%로 고정되어 있어서

정답: ③ (너무 많으면 원본을 잃어버리고, 너무 적으면 충분히 학습이 안 되므로, 경험적으로 15%가 양방향 문맥 학습에 최적의 비율이다)

**32. Seq2Seq 모델의 Attention 메커니즘이 해결하는 문제는?**

① 학습 시간이 길어지는 문제
② 긴 입력 문장의 정보가 하나의 벡터에 압축되면서 손실되는 문제
③ 메모리 사용량이 많은 문제
④ 활성함수 선택 문제

정답: ② (Seq2Seq는 입력 전체를 하나의 context vector로 압축하는데, 긴 문장에서 정보가 손실된다. Attention은 디코딩할 때 필요한 입력 부분만 동적으로 선택하여 이를 해결한다)

**33. Transformer의 Self-Attention에서 Positional Encoding이 필요한 이유는?**

① 계산 속도를 높이기 위해
② Self-Attention이 단어 간 유사도만 계산하므로 순서 정보가 부족하기 때문
③ 모델의 크기를 줄이기 위해
④ 정규화 효과를 위해

정답: ② (Self-Attention은 각 토큰 쌍의 유사도(Q·K)만 계산하므로 순서 정보가 없다. Positional Encoding으로 위치 정보를 임베딩에 추가한다)

---

### 4차시

**34. Zero-shot CoT에서 "Let's think step by step"이라는 프롬프트를 추가하면 성능이 향상되는 이유는?**

① 모델의 파라미터가 변경되어서
② 모델이 단계적 추론을 수행하도록 유도하기 때문
③ 데이터가 증가되어서
④ 손실함수가 변경되어서

정답: ② (이 프롬프트는 모델에게 "천천히 생각해서 답해"라는 지시를 하므로, 모델이 중간 단계를 명시적으로 나타내며 추론하게 되어 수학/논리 문제에서 성능이 크게 향상된다)

**35. Greedy Decoding에서 매 시점마다 확률이 가장 높은 토큰만 선택하면 안 되는 이유는?**

① 계산 속도가 느려져서
② 각 단계가 최적이어도 전체 문장은 최적이 아닐 수 있기 때문
③ 메모리를 너무 많이 사용해서
④ 활성함수와 호환되지 않아서

정답: ② (Greedy 방식은 각 단계에서의 국소 최적(local optimum)을 선택하므로, 전체 문장의 확률은 더 낮을 수 있다. 이를 지역 최적에 빠진다고 표현한다)

---

### 5차시

**36. CNN에서 출력 크기 공식 (W - F + 2P) / S + 1에서 "Same 패딩"을 사용할 때의 조건은?**

① P = F이면 됨
② P = (F - 1) / 2이고 S = 1일 때
③ S = 0일 때
④ 항상 자동으로 적용됨

정답: ② ("Same 패딩"은 출력 크기를 입력과 같게 유지하는데, P = (F-1)/2이고 스트라이드가 1일 때 가능하다)

**37. ResNet의 Skip Connection이 기울기 소실 문제를 해결하는 메커니즘은?**

① ReLU 활성함수를 사용해서
② 기울기가 skip connection을 통해 입력층으로 직접 전달되기 때문
③ 필터 개수를 줄여서
④ Batch Normalization을 사용해서

정답: ② (Skip connection으로 기울기가 직접 역전파될 수 있어서 기울기 소실이 방지된다. 3차시 LSTM의 Cell State와 같은 원리다)

**38. Global Average Pooling이 FC 층을 완전히 대체할 수 있는 이유는?**

① 계산이 더 빠르기 때문
② 각 특징 맵 전체를 하나의 값으로 평균하여 파라미터를 극도로 줄일 수 있기 때문
③ 더 정확한 분류를 하기 때문
④ 모든 데이터에 적용 가능하기 때문

정답: ② (GAP는 H×W×C 특징 맵을 1×1×C로 축소하므로, 이후 FC의 입력 차원이 매우 작아져 파라미터를 거의 없앨 수 있다)

---

### 6차시

**39. CLIP의 대조 학습(Contrastive Learning)에서 배치 내 유사도 행렬에서 대각선 원소들을 높게 유지하는 이유는?**

① 대각선이 맞는 이미지-텍스트 쌍이기 때문
② 정규화를 위해
③ 계산 속도를 높이기 위해
④ 데이터 증강 효과를 위해

정답: ① (대각선 원소(i,i)는 이미지i와 텍스트i의 쌍으로, 이들의 유사도를 높게 유지하는 것이 학습 목표다)

**40. SigLIP이 CLIP의 Softmax 손실을 Sigmoid 손실로 변경한 이점은?**

① 더 빠른 추론 속도
② 각 쌍을 독립적으로 판단하므로 큰 배치가 필수가 아니고 더 효율적
③ 더 정확한 분류
④ 메모리 사용량이 증가

정답: ② (Softmax는 배치 내 정규화가 필요해 큰 배치(32K)가 필수지만, Sigmoid는 각 쌍의 이진 분류이므로 배치 크기에 덜 의존한다)

**41. Diffusion 모델에서 Forward Process의 목표는?**

① 이미지에 노이즈를 단계적으로 추가하는 것
② 노이즈에서 이미지를 복원하는 방법을 학습하는 것
③ 노이즈를 제거하는 신경망을 학습하는 것
④ 이미지를 압축하는 것

정답: ① (Forward Process는 원본 이미지에 정해진 스케줄에 따라 단계적으로 가우시안 노이즈를 추가하는 과정으로, Reverse Process를 학습하기 위한 준비 단계다)

**42. Text-to-Image 생성에서 CLIP의 역할은?**

① 이미지에 글씨를 추가하기
② 텍스트 설명을 벡터 임베딩으로 변환하여 Diffusion 모델의 조건으로 제공
③ 이미지 품질을 평가하기
④ 노이즈를 제거하기

정답: ② (CLIP의 Text Encoder가 텍스트를 임베딩으로 변환하고, 이 벡터를 Diffusion의 조건부 정보로 사용하여 텍스트에 맞는 이미지를 생성한다)

**43. 파운데이션 모델의 "창발적 능력(Emergent Ability)"의 예시로 가장 적합한 것은?**

① 더 깊은 신경망을 사용하면 항상 성능이 조금씩 향상된다
② 모델 크기 10B 이하에서는 불가능하지만 100B 이상에서는 갑자기 few-shot learning이 가능해진다
③ 학습 데이터가 많을수록 선형적으로 성능이 향상된다
④ 활성함수를 바꾸면 성능이 향상된다

정답: ② (창발적 능력은 일정 규모를 넘으면 갑자기 나타나는 능력으로, 3~4차시의 In-Context Learning이 대표적 예시다)

**44. Hugging Face Transformers 라이브러리의 주요 역할은?**

① GPU 하드웨어를 관리하기
② 사전학습된 모델을 쉽게 로드하고 사용할 수 있는 Python 라이브러리 제공
③ 데이터 수집 및 라벨링
④ 모델 배포 서버 운영

정답: ② (Transformers 라이브러리는 BERT, GPT 등 수백만 개의 사전학습 모델을 쉽게 접근할 수 있게 하는 핵심 도구다)

---

## 연결 문제 (고급)

**45. 1차시의 오버피팅 개념이 2차시의 정규화, 3차시의 드롭아웃, 5차시의 스트라이드/풀링과 어떻게 연결되는지 설명하시오.**

답: 모두 모델의 복잡도를 제한하거나 일반화를 강화하여 오버피팅을 방지한다. 정규화는 가중치 크기에 페널티, 드롭아웃은 뉴런을 랜덤하게 제거, 스트라이드/풀링은 파라미터 수를 줄인다.

**46. 3차시 Transformer의 Self-Attention과 4차시의 CLIP에서 활용된 Attention의 본질적 차이는 무엇인가?**

답: Transformer의 Self-Attention은 "텍스트 토큰들 간의 관계"를 학습하고, CLIP의 Attention(사실 Self-Attention의 변형)은 "이미지와 텍스트 임베딩 간의 유사도"를 계산하는 데 사용된다. 두 모두 관계 학습의 메커니즘이지만 대상이 다르다.

---

## 답안지 요약

**주관식 답안 요약:**
- 1-4: AI 기초 개념 (AI/ML/DL 정의, 혼동행렬, R², k-means)
- 5-8: 회귀/분류/신경망 (최소제곱법, 시그모이드, 활성함수, 경사하강법)
- 9-13: NLP 기초 (원핫/임베딩, RNN 기울기 소실, LSTM, Transformer, BERT vs GPT)
- 14-15: LLM (ICL, Beam Search)
- 16-18: CNN (가중치 공유, 합성곱 공식, Pooling)
- 19-20: 멀티모달 (CLIP, Diffusion vs GAN)

**객관식 답안 요약:**
- 21-24: 1차시 (MSE, 테스트 셋, F1 Score, k-means)
- 25-28: 2차시 (로지스틱 회귀, 경사하강법, 역전파, 신경망 깊이)
- 29-33: 3차시 (Skip-Gram, Perplexity, MLM, Attention, Positional Encoding)
- 34-35: 4차시 (CoT, Greedy Decoding)
- 36-38: 5차시 (Same Padding, Skip Connection, Global Average Pooling)
- 39-44: 6차시 (CLIP 대조학습, SigLip, Diffusion, Text-to-Image, 파운데이션 모델, Hugging Face)

