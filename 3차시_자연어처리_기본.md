# 3차시 - 자연어 처리 기본

> 2차시에서 배운 신경망을 **텍스트(순차 데이터)**에 적용하는 방법을 다룬다. 텍스트를 숫자로 바꾸는 방법(임베딩)부터, 순차 데이터를 처리하는 RNN/LSTM, 그리고 이를 혁신적으로 대체한 Transformer와 BERT까지, NLP의 핵심 흐름을 학습한다.

## 1. 텍스트 표현

컴퓨터는 숫자만 처리할 수 있으므로, 단어를 **숫자 벡터**로 바꿔야 한다. 이 변환 방법에 따라 모델 성능이 크게 달라진다.

### 원핫 인코딩 (One-Hot Encoding)

규칙 기반/통계적 NLP에서 단어를 **원자적(쪼갤 수 없는) 기호**로 취급하는 방법. 한 원소만 1이고 나머지 모두 0인 벡터.

```
예: 사전 = [나, 너, 사랑, 하다, 좋다]  (사전 크기 = 5)

"사랑" = [0, 0, 1, 0, 0]
"좋다" = [0, 0, 0, 0, 1]
"나"   = [1, 0, 0, 0, 0]
```

- 차원수 = 단어 사전 크기 (실제 사전은 수만~수십만 단어)

**단점**:
1. **차원의 저주**: 사전이 5만 단어면 5만 차원 벡터. 대부분이 0인 희소(sparse) 벡터 → 메모리 비효율
2. **의미적 정보 부족**: "사랑"과 "좋다"는 비슷한 의미인데도 벡터 간 유사도가 0 (직교). 모든 단어 쌍이 동일한 거리에 있음

### 워드 임베딩 (Word Embedding)

단어를 **저차원의 밀집(dense) 벡터**로 표현하여 의미적 관계를 포착하는 방법

```
원핫: "사랑" = [0, 0, 1, 0, 0]          (5차원, 희소)
임베딩: "사랑" = [0.82, -0.15, 0.43, ...]  (예: 300차원, 밀집)
```

- 비슷한 의미의 단어는 벡터 공간에서 **가까이** 위치
- 유명한 예: "King" - "Man" + "Woman" ≈ "Queen" (의미 연산 가능)

#### Word2Vec의 두 가지 방법

| 방식 | 학습 방향 | 특징 | 적합한 상황 |
|------|----------|------|-----------|
| **Skip-Gram (SG)** | 중심 단어 → 주변 단어 예측 | 희귀 단어 표현 잘 학습 | 데이터 적을 때, 희귀 단어 중요할 때 |
| **CBOW** | 주변 단어 → 중심 단어 예측 | 학습 속도 빠름, 빈도 높은 단어에 강함 | 데이터 많을 때, 빠른 학습 필요 |

```
예문: "I love NLP"

Skip-Gram: 중심 "love" → 주변 "I", "NLP" 예측
CBOW:      주변 "I", "NLP" → 중심 "love" 예측
```

### 연결 포인트

> 원핫 인코딩의 한계를 극복한 것이 워드 임베딩이고, 이 임베딩은 이후 RNN, Transformer, BERT 등 모든 NLP 모델의 **입력**으로 사용된다. BERT는 문맥에 따라 임베딩이 달라지는 **문맥적 임베딩(contextual embedding)**을 제공한다.

---

## 2. RNN (Recurrent Neural Network) 원리 이해

### 순차적 데이터란?

| 특징 | 설명 | 예시 |
|------|------|------|
| **순서가 중요** | 순서 바뀌면 의미 달라짐 | "개가 사람을 물었다" vs "사람이 개를 물었다" |
| **장기 의존성** | 멀리 떨어진 과거 정보가 현재에 영향 | "나는 프랑스에서 자랐기 때문에 ... _____를 잘한다" → "프랑스어" |
| **가변 길이** | 데이터마다 길이가 다름 | 문장 길이가 제각각 |

> 2차시의 MLP(일반 신경망)는 **고정 길이 입력**만 받고, **순서 정보를 반영하지 못함** → 순차 데이터 처리에 RNN이 필요

### RNN 구조와 원리

```
시점 1:  x₁ → [RNN] → h₁
                        ↓
시점 2:  x₂ → [RNN] → h₂
                        ↓
시점 3:  x₃ → [RNN] → h₃  → 출력
```

- **Hidden state (hₜ)**: 이전 시점까지의 정보를 요약한 벡터
- 매 시점마다 "현재 입력(xₜ)"과 "이전 기억(hₜ₋₁)"을 결합하여 새 hidden state 생성

```
hₜ = f(W_h · hₜ₋₁ + W_x · xₜ + b)

W_h : hidden state에 곱하는 가중치 (기억에 대한 가중치)
W_x : 입력에 곱하는 가중치
b   : 편향
f   : 활성함수 (보통 tanh)
```

### RNN의 핵심 특징

- 같은 가중치(W_h, W_x)를 **모든 시점에서 공유** → 파라미터 효율적
- 이론적으로 **임의 길이의 시퀀스** 처리 가능
- 펼쳐서 보면, 각 층이 하나의 시점을 나타내는 **매우 깊은 신경망**처럼 보임

### 기울기 소실 문제 (Vanishing Gradient)

- **문제**: 시퀀스가 길어지면 초기 시점의 정보가 사라짐
- **원인**: 역전파 과정에서 기울기가 **여러 시점을 거치며 반복 곱셈** → 1보다 작은 값이 계속 곱해지면 0에 수렴
- **결과**: 장기 의존성 학습 실패. "나는 프랑스에서 자랐기 때문에 ... 프랑스어를 잘한다"에서 "프랑스"를 기억하지 못함

```
기울기 역전파:
h₃ ← h₂ ← h₁

각 단계에서 W_h가 곱해짐:
∂L/∂h₁ = ... × W_h × W_h × W_h

W_h의 값이 1보다 작으면 → 기울기 소실 (vanishing)
W_h의 값이 1보다 크면 → 기울기 폭발 (exploding)
```

> 이 문제를 해결하기 위해 LSTM이 등장했다.

---

## 3. LSTM (Long Short-Term Memory) 원리 이해

1997년 Hochreiter & Schmidhuber가 제안. RNN의 기울기 소실 문제를 **게이트 메커니즘**으로 해결.

### 핵심 아이디어: 기억의 고속도로

- RNN: 정보가 hidden state 하나를 통해 전달 → 오래되면 소실
- LSTM: **Cell State**라는 별도의 **정보 고속도로**를 추가 → 정보가 거의 변하지 않고 먼 거리를 흐를 수 있음

### 두 가지 상태

| 상태 | 역할 | 비유 |
|------|------|------|
| **Cell State (Cₜ)** | **장기 기억** 저장소. 컨베이어 벨트처럼 정보가 흐름 | 노트에 적어둔 메모 |
| **Hidden State (hₜ)** | **단기 기억** / 현재 출력. 다음 시점으로 전달 | 지금 머릿속에 떠올린 것 |

### 3개의 게이트 (모두 시그모이드 사용 → 0~1 출력)

| 게이트 | 수식 | 역할 | 동작 |
|--------|------|------|------|
| **Forget Gate (fₜ)** | fₜ = σ(W_f·[hₜ₋₁, xₜ] + b_f) | 이전 기억 중 **버릴 것** 결정 | 0→삭제, 1→유지 |
| **Input Gate (iₜ)** | iₜ = σ(W_i·[hₜ₋₁, xₜ] + b_i) | 새 정보 중 **저장할 것** 결정 | 0→무시, 1→저장 |
| **Output Gate (oₜ)** | oₜ = σ(W_o·[hₜ₋₁, xₜ] + b_o) | Cell에서 **출력할 것** 결정 | 0→차단, 1→출력 |

### LSTM 동작 흐름 (4단계)

```
1. Forget: Cₜ₋₁ 중 불필요한 것 삭제
   Cₜ₋₁ × fₜ

2. Input: 새 후보 정보 생성 + 얼마나 저장할지 결정
   후보: C̃ₜ = tanh(W_c·[hₜ₋₁, xₜ] + b_c)
   저장량: iₜ × C̃ₜ

3. Cell Update: 이전 기억 일부 삭제 + 새 정보 추가
   Cₜ = fₜ × Cₜ₋₁ + iₜ × C̃ₜ

4. Output: Cell에서 필요한 부분만 꺼내 출력
   hₜ = oₜ × tanh(Cₜ)
```

### 왜 기울기 소실이 해결되는가?

- Cell State 업데이트가 **덧셈** 기반: Cₜ = fₜ × Cₜ₋₁ + iₜ × C̃ₜ
- RNN은 매번 행렬 **곱셈** → 기울기가 기하급수적으로 줄어듦
- LSTM은 **덧셈 경로**가 있어서 기울기가 비교적 잘 보존됨

### LSTM 응용 예시

```
"나는 프랑스에서 자랐기 때문에 ... 프랑스어를 잘한다"

Forget Gate: "프랑스" 정보를 유지 (삭제하지 않음)
Input Gate: 중간의 덜 중요한 단어들은 적게 저장
Output Gate: "프랑스어"를 예측할 때 Cell에서 "프랑스" 정보를 꺼냄
```

---

## 4. NLP 성능평가

### 언어 모델 (Language Model)

- 인간의 두뇌가 자연어를 생성하는 능력을 모방한 모델
- 단어 시퀀스 전체에 **확률을 부여**하여 문장의 자연스러움을 측정
- 한 문장의 확률 = 각 단어의 **조건부 확률들의 곱** (체인 룰)

```
P("I love NLP") = P(I) × P(love|I) × P(NLP|I,love)
```

### N-gram 모델

연속된 N개의 단어 묶음으로 문맥을 파악하는 **가장 단순한 언어 모델**

| 종류 | 예시 ("I love NLP") | 조건부 확률 |
|------|-------------------|-----------|
| 1-gram (unigram) | I / love / NLP | P(love) — 독립적 |
| 2-gram (bigram) | I love / love NLP | P(love\|I) — 직전 1단어만 봄 |
| 3-gram (trigram) | I love NLP | P(NLP\|I,love) — 직전 2단어 봄 |

- **장점**: 구현 간단, 계산 빠름
- **단점**: N이 커지면 데이터 부족 (희소성), 긴 문맥 반영 어려움
- **한계**: N-gram은 "고정된 창"만 봄 → RNN/Transformer가 이를 해결

### Perplexity (혼란도)

- 언어 모델의 성능을 평가하는 **대표 지표**
- **값이 낮을수록** 모델이 다음 단어를 잘 예측함

```
PPL = 2^(-1/N × Σlog₂P(wᵢ|w₁...wᵢ₋₁))
```

- 직관: "모델이 다음 단어를 예측할 때 평균적으로 **몇 개의 후보 중에서 고민하는가**"
- PPL = 10이면, 매 단어마다 10개 후보 중에서 선택하는 수준
- PPL = 1이면, 완벽하게 예측 (실제로는 불가능)

### BLEU Score

- **기계 번역 품질** 평가 지표
- 생성된 문장이 참조 문장과 **N-gram 단위로 얼마나 겹치는지** 측정

```
BLEU = BP × exp(Σ wₙ × log(precisionₙ))

BP: Brevity Penalty (너무 짧은 문장 페널티)
precisionₙ: n-gram 정밀도
```

- 0~1 범위, 1에 가까울수록 좋은 번역
- 한계: 의미는 같지만 표현이 다른 번역을 낮게 평가할 수 있음

### ROUGE Score

- **텍스트 요약 품질** 평가 지표
- BLEU가 Precision 기반이라면, ROUGE는 **Recall 기반**
- "참조 요약의 N-gram이 생성 요약에 얼마나 포함되는가"

| 지표 | 관점 | 주 사용처 |
|------|------|----------|
| BLEU | 정밀도 (생성 문장 기준) | 번역 |
| ROUGE | 재현율 (참조 문장 기준) | 요약 |

---

## 5. Seq2Seq, Attention, 디코딩

### Seq2Seq (Sequence-to-Sequence)

하나의 시퀀스를 입력받아 **다른 시퀀스를 출력**하는 모델. 번역, 요약, 챗봇 등에 사용.

| 구성 | 역할 | 구조 |
|------|------|------|
| **Encoder** | 입력 시퀀스를 읽어 **하나의 벡터(context vector)**로 압축 | RNN/LSTM |
| **Decoder** | Context vector를 바탕으로 출력 시퀀스를 **한 단어씩** 생성 | RNN/LSTM |

```
입력: "I love you"
→ Encoder가 전체 의미를 벡터 하나로 압축
→ Decoder가 "나는", "너를", "사랑해"를 순차적으로 생성
```

- **입력/출력 길이가 달라도** 처리 가능 (번역에서 중요!)
- **한계**: 긴 문장일수록 하나의 벡터에 정보를 다 담기 어려움 → 정보 손실

### Attention 메커니즘

Seq2Seq의 정보 손실 문제를 해결. **디코딩할 때 입력 전체를 한 번에 압축하지 않고, 필요한 부분만 골라 보는 방법**

| 단계 | 동작 | 설명 |
|------|------|------|
| 1 | Encoder가 hidden state 생성 | 입력 각 단어마다 h₁, h₂, h₃, ... |
| 2 | 유사도 계산 | Decoder 현재 상태와 Encoder 각 hidden state의 유사도 |
| 3 | Attention weight (가중치) | softmax로 정규화 → 중요한 단어에 높은 값 |
| 4 | Context vector | 가중치를 반영해 Encoder hidden state들을 가중합 |
| 5 | 출력 생성 | Context vector + Decoder 상태 → 다음 단어 예측 |

```
"I love you" → "나는 너를 사랑해"

"나는" 생성 시 → "I"에 높은 attention weight
"너를" 생성 시 → "you"에 높은 attention weight
"사랑해" 생성 시 → "love"에 높은 attention weight
```

> Attention의 핵심: 매 출력 시점마다 **"어디를 볼지"를 동적으로 결정**

### Beam Search (디코딩 알고리즘)

Decoder가 단어를 생성할 때의 탐색 전략 (4차시에서 더 자세히 다룸)

| 전략 | 방식 | 특징 |
|------|------|------|
| **Greedy** | 매번 확률 최고 1개 선택 | 빠르지만 비최적 |
| **Beam Search** | 상위 k개 후보 유지하며 탐색 | 더 좋은 문장 가능 |

### 연결 포인트

> Seq2Seq의 Attention은 **Transformer의 직접적 조상**이다. Transformer는 "Attention만으로 충분하다(Attention Is All You Need)"는 아이디어로 RNN을 완전히 대체했다.

---

## 6. Transformer 원리 이해

### RNN의 한계 → Transformer의 등장 배경

| RNN의 한계 | Transformer의 해결 |
|------------|-------------------|
| **장기 의존성**: 먼 단어 쌍의 관계 학습 어려움 | Self-Attention으로 **모든 단어를 직접 참조** |
| **병렬화 불가**: 순차 처리라 시점 1→2→3 순서 필수 | 전체 문장을 **한 번에 병렬 처리** (GPU 효율적) |
| **기울기 소실**: LSTM으로 완화했지만 완전 해결은 아님 | Attention에는 곱셈 연쇄가 없어 기울기 잘 전달 |

### Self-Attention

하나의 문장 안에서 **단어끼리 서로를 참고**해서 문맥을 반영한 새 표현을 만드는 방식

```
예: "The animal didn't cross the street because it was tired"

"it"이 무엇인지 판단할 때:
→ "animal"에 높은 attention (it = animal이라는 것을 학습)
→ "street"에는 낮은 attention
```

#### Q, K, V 개념

각 단어에서 3개의 벡터를 생성:

| 벡터 | 의미 | 비유 |
|------|------|------|
| **Q (Query)** | "내가 무엇을 찾는지" | 도서관에서 검색하는 질문 |
| **K (Key)** | "내가 어떤 정보를 가졌는지" | 각 책의 제목/키워드 |
| **V (Value)** | "실제 전달할 정보" | 책의 내용 |

#### 동작 과정

```
1. 유사도 계산: score = Q · Kᵀ (내적)
2. 스케일링: score / √d_k (차원수의 제곱근으로 나눔 → 안정화)
3. 정규화: softmax(score) → attention weights (합이 1)
4. 가중합: weights × V → 문맥 반영된 새로운 표현
```

### Multi-Head Attention

- Self-Attention을 **여러 개(h개) 동시에** 수행
- 각 head가 **다른 관점**에서 관계를 학습
- 예: head 1은 문법적 관계, head 2는 의미적 관계, head 3은 위치 관계...
- 모든 head의 결과를 합쳐서 최종 표현 생성

### Self-Attention의 3가지 한계와 해결

| 한계 | 설명 | 해결 방법 |
|------|------|----------|
| **순서 정보 부재** | 단어 간 유사도만 계산, 순서를 모름 | **Positional Encoding** — 위치 정보를 벡터에 더함 |
| **비선형성 부족** | Attention은 가중합이라 선형 결합 | **Feed-Forward Network** — 각 토큰에 비선형 변환 적용 |
| **미래 참조 문제** | 모든 단어를 동시에 봄 → 아직 생성 안 된 단어도 참조 | **Masked Self-Attention** — 미래 위치를 마스킹 |

### Transformer 전체 구조

| Encoder (입력 처리) | Decoder (출력 생성) |
|---------|---------|
| Self-Attention + FFN | **Masked** Self-Attention |
| 입력 전체를 양방향으로 봄 | 현재까지 생성된 것만 봄 (왼쪽→오른쪽) |
| | + Encoder-Decoder Attention (Encoder 출력 참조) |
| | + FFN |

- 각 층마다 **Residual Connection + Layer Normalization** 적용
- Encoder/Decoder 모두 **여러 층(보통 6~12층)** 쌓음

> Transformer = "Self-Attention으로 문맥을 학습하는 병렬 시퀀스 모델"

### 연결 포인트

> Transformer의 Encoder만 사용 → **BERT** (양방향 이해)
> Transformer의 Decoder만 사용 → **GPT** (텍스트 생성)
> Encoder + Decoder → **번역 모델** (T5, BART)
> 4차시에서 배울 **Decoder 모델(GPT 계열)**과 **In-Context Learning**은 Transformer의 Decoder 구조를 기반으로 한다.

---

## 7. BERT 원리 이해

### 기본 개념

- Transformer **Encoder만 사용**한 모델
- 문장을 **양방향(bidirectional)**으로 이해
- 목적: 범용적인 언어 이해 능력을 사전학습

### 기존 모델과의 차이

| 모델 | 방향 | 한계 |
|------|------|------|
| GPT (Decoder) | 왼쪽→오른쪽만 | 오른쪽 문맥을 활용 못함 |
| ELMo | 양방향 별도 학습 후 결합 | 깊은 양방향 상호작용 부족 |
| **BERT** | **진정한 양방향** | 양쪽 문맥을 동시에 고려 |

### 사전학습 (Pre-training) 방법

#### 1. MLM (Masked Language Model)

문장의 일부 단어를 **[MASK]**로 가리고, 주변 문맥을 보고 예측

```
입력: "I [MASK] NLP"
정답: "love"

→ 왼쪽("I")과 오른쪽("NLP") 문맥을 모두 활용하여 "love"를 맞춤
```

- 전체 토큰의 약 15%를 마스킹
- 양방향 문맥을 학습하는 핵심 방법

#### 2. NSP (Next Sentence Prediction)

두 문장이 실제로 이어지는지 판단 (이진 분류)

```
입력: [CLS] 나는 밥을 먹었다 [SEP] 배가 부르다 [SEP]
정답: IsNext (True)

입력: [CLS] 나는 밥을 먹었다 [SEP] 오늘 날씨가 좋다 [SEP]
정답: NotNext (False)
```

- [CLS]: 문장 시작 토큰 (분류에 사용)
- [SEP]: 문장 구분 토큰
- 문장 간 관계를 이해하는 능력 학습

### 파인튜닝 (Fine-tuning)

사전학습된 BERT를 **특정 작업에 맞게 추가 학습**. 소량의 데이터로도 높은 성능.

| 작업 | 입력 | 출력 |
|------|------|------|
| 감정 분석 | 문장 | 긍정/부정 |
| 문장 분류 | 문장 | 카테고리 |
| 개체명 인식 (NER) | 문장 | 각 토큰의 태그 |
| 질의응답 (QA) | 질문 + 지문 | 답변 위치 |

### 사전학습-파인튜닝 패러다임

```
대규모 비라벨 데이터로 사전학습 (비용 큼, 한 번만)
        ↓
소규모 라벨 데이터로 파인튜닝 (작업별로 쉽고 빠르게)
```

> 이 패러다임이 NLP를 혁신했고, 4차시의 GPT/LLM에서는 **파인튜닝 없이 프롬프트만으로** 작업을 수행하는 방향으로 진화한다 (In-Context Learning).

> BERT = "마스킹된 단어 예측과 문장 관계 학습으로 문맥을 이해하는 양방향 모델"

---

## 차시 연결 요약

> 3차시의 NLP 모델들은 **진화 순서**로 연결된다:
>
> **원핫** → **워드 임베딩** (의미 반영) → **RNN** (순서 반영) → **LSTM** (장기 기억) → **Seq2Seq+Attention** (번역/생성) → **Transformer** (병렬+장거리) → **BERT** (양방향 이해)
>
> 4차시에서는 Transformer **Decoder 기반** 모델(GPT 계열)과, 이를 활용하는 **디코딩 알고리즘**, **In-Context Learning**, **선호 학습**을 다룬다.
