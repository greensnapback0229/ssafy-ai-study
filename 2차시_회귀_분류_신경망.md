# 2차시 - 회귀, 분류, 신경망

> 1차시에서 회귀/분류의 **개념과 평가 지표**를 배웠다면, 2차시는 이를 실제로 풀기 위한 **모델과 학습 방법**을 다룬다. 선형회귀 → 로지스틱 회귀 → 신경망으로 점점 복잡한 모델을 배우며, 이 모델들을 학습시키는 공통 원리인 **손실함수 + 경사하강법**이 핵심이다.

## 1. 선형회귀

입력 변수와 출력 변수 사이의 관계를 **직선 형태**로 근사해서 예측하는 방법. 1차시에서 배운 **회귀 문제**를 푸는 가장 기본적인 모델이다.

### 단순선형회귀

한 개의 설명 변수(X)와 하나의 반응변수(Y)의 직선 관계를 찾는 방법

```
Y = b₀ + b₁X + e

b₀ = 절편 (X가 0일 때 Y의 평균 추정값)
b₁ = 기울기 (X가 1 증가할 때 Y의 평균 변화량)
e  = 관측 오차 (잔차)
```

**예시**: 집 크기(X)로 집 가격(Y) 예측
- b₀ = 5000만원 (크기 0이어도 기본 가격)
- b₁ = 200만원 (1㎡ 늘어날 때 가격 증가량)
- Y = 5000 + 200 × 30 = 11,000만원 (30㎡ 집의 예상 가격)

### 다중선형회귀

독립 변수(Feature)가 **여러 개** 존재할 때 사용하는 회귀 분석

```
Y = b₀ + b₁X₁ + b₂X₂ + ... + bₚXₚ + e
```

- 해석: 다른 변수를 고정했을 때, 특정 Xⱼ가 1 증가하면 Y는 평균적으로 bⱼ만큼 변화
- 예: 집 가격 = b₀ + b₁×(크기) + b₂×(방 수) + b₃×(역까지 거리) + e

### 주의사항

- **상관 ≠ 인과**: 관찰 데이터의 상관관계로 인과 관계를 주장하면 안 됨
  - 예: 아이스크림 판매↑와 익사 사고↑는 상관 있지만, 원인은 "여름 기온"
- **다중공선성**: 변수들이 서로 강하게 연관되어 있으면 계수 추정이 불안정해짐
- 이상적 상황: 변수들이 서로 독립적일 때 계수 해석이 명확함

---

## 2. RSS (잔차제곱합)와 최소제곱법

### RSS (Residual Sum of Squares) 정의

```
RSS = Σ(yᵢ - ŷᵢ)² = Σ(잔차)²
```

- **잔차(Residual)** = 실제 관측값(yᵢ) - 예측값(ŷᵢ)
- RSS는 모든 데이터 포인트의 잔차를 제곱해서 합한 값
- RSS가 작을수록 모델이 데이터를 잘 설명함

### RSS와 1차시 개념의 연결

| 1차시 | 2차시 | 관계 |
|-------|-------|------|
| MSE | RSS | MSE = RSS / n (RSS를 데이터 수로 나눈 것) |
| R² = 1 - RSS/TSS | RSS 최소화 | RSS를 줄이면 R²가 올라감 |

### 최소제곱법 (Ordinary Least Squares, OLS)

- RSS를 **최소화**하는 절편(b₀)과 기울기(b₁)를 찾는 방법
- 데이터와 회귀선 간의 차이(잔차)를 최소화하는 원리
- 수학적으로 RSS를 b₀, b₁에 대해 미분하여 0이 되는 점을 찾음

```
최적 기울기: b₁ = Σ(xᵢ - x̄)(yᵢ - ȳ) / Σ(xᵢ - x̄)²
최적 절편:   b₀ = ȳ - b₁x̄
```

> 핵심: "잔차제곱합을 최소화하여 최적의 직선을 찾는다"

### 훈련 vs 테스트 성능

- 최소제곱해는 **훈련 데이터만** 보고 계산됨
- 훈련 데이터에서의 RSS가 작다고 좋은 모델이 아님 (1차시의 오버피팅!)
- 일반화 성능 확인을 위해 **훈련에 사용되지 않은 새 데이터**로 평가 필요
- 변수가 많거나 고차항 사용 시 **과적합** 발생 가능 → 정규화 필요

---

## 3. 로지스틱 회귀 (Logistic Regression)

> 1차시에서 배운 **분류 문제**를 풀기 위한 모델. 이름에 "회귀"가 있지만 실제로는 **분류 모델**이다.

### 분류란?

- 정해진 범주 중 하나로 지정하는 것 (1차시의 혼동행렬로 평가)
- 입력: 특성 벡터 (Feature) → 출력: 순서 없는 범주 집합
- 범주의 직접 예측보다 **범주에 속할 확률을 추정**하는 것이 유용할 때가 많음

### 왜 분류에 선형회귀를 쓰면 안 되는가?

- 선형회귀는 예측 값이 0~1 범위로 제한되지 않음
- 확률은 반드시 0~1 사이여야 하는데, 선형회귀는 -∞ ~ +∞ 출력
- → 출력을 0~1로 압축하는 함수가 필요 → **시그모이드 함수**

### 시그모이드 함수

```
σ(z) = 1 / (1 + e⁻ᶻ)
```

- 어떤 실수값(z)이든 0~1 사이 값으로 변환
- z가 매우 크면 → 1에 가까움 (확률 높음)
- z가 매우 작으면 → 0에 가까움 (확률 낮음)
- z = 0이면 → 정확히 0.5

> 시그모이드는 이후 신경망의 **활성함수**로도 등장하고, 3차시 LSTM의 **게이트**에서도 사용된다.

### 로지스틱 회귀 모형

```
P(Y=1|X) = σ(b₀ + b₁X) = 1 / (1 + e^-(b₀ + b₁X))
```

- 선형회귀의 출력(b₀ + b₁X)을 시그모이드에 통과시킨 것
- 출력이 0.5 이상이면 → 양성(Positive)으로 분류
- 출력이 0.5 미만이면 → 음성(Negative)으로 분류

### 오즈 (Odds)

```
Odds = 성공확률 / 실패확률 = P / (1-P)
```

- 성공 확률이 실패 확률에 비해 **몇 배 더 높은가**를 나타냄
- 예: P=0.75 → Odds = 0.75/0.25 = 3 (성공이 실패의 3배)
- 로그 오즈(Log Odds) = log(Odds) = b₀ + b₁X → 이것이 선형!

### 우도 (Likelihood)와 학습

| 선형회귀 | 로지스틱 회귀 |
|----------|-------------|
| MSE(RSS)를 **최소화** | 우도(Likelihood)를 **최대화** |
| "오차가 작은가?" | "현재 확률 함수가 데이터를 잘 설명하는가?" |
| 닫힌 해(closed-form) 존재 | 경사하강법으로 반복 최적화 필요 |

> 우도란: 현재 모델 파라미터(b₀, b₁) 하에서 관측된 데이터가 나타날 확률. 이를 최대화하면 데이터를 가장 잘 설명하는 파라미터를 찾을 수 있다.

---

## 4. 손실함수 (Loss Function)

> 모든 ML 모델 학습의 공통 구조: **손실함수 정의 → 손실을 최소화하는 파라미터 탐색**

### 정의

- 모델이 **얼마나 잘못 예측하는지** 측정하는 함수
- 값이 작을수록 더 정확하게 학습되었다는 의미
- 학습 = 손실함수를 최소화하는 파라미터를 찾는 것

### 학습 과정

```
1. 학습 데이터: (입력, 정답) 쌍 I개
2. 모델이 입력에 대해 예측값 계산
3. 손실함수로 예측값과 정답의 차이 계산
4. 손실을 줄이는 방향으로 파라미터 업데이트 (경사하강법)
5. 반복
```

### 주요 손실함수

| 손실함수 | 수식 | 사용처 | 직관 |
|---------|------|--------|------|
| **MSE** | (1/n) Σ(yᵢ - ŷᵢ)² | 회귀 문제 | 예측값과 실제값의 차이를 제곱 |
| **Cross-Entropy** | -Σ[y·log(ŷ) + (1-y)·log(1-ŷ)] | 분류 문제 | 예측 확률이 정답에서 얼마나 먼지 |

### Cross-Entropy의 직관

- 정답이 1일 때: 모델이 1에 가까운 확률을 내면 손실 작음, 0에 가까우면 손실 큼
- 정답이 0일 때: 모델이 0에 가까운 확률을 내면 손실 작음, 1에 가까우면 손실 큼
- 로지스틱 회귀의 우도 최대화 = Cross-Entropy 최소화 (같은 것!)

---

## 5. 경사하강법 (Gradient Descent)

> 손실함수를 최소화하기 위한 **최적화 알고리즘**. 선형회귀부터 딥러닝까지 거의 모든 모델 학습에 사용된다.

### 핵심 아이디어

손실을 줄이기 위해 파라미터(w)를 어떻게 바꿀까? → **기울기(gradient) 반대 방향으로 이동**

### 수식

```
w = w - η × (dL/dw)

w     : 현재 가중치
η     : 학습률 (learning rate) — 한 번에 얼마나 이동할지
dL/dw : 손실함수의 기울기 (gradient)
```

### 직관적 이해

산에서 가장 낮은 골짜기를 찾는 것에 비유:
- 현재 위치에서 **경사(기울기)**를 확인
- 경사가 내리막인 방향으로 한 걸음 이동
- 이를 반복하면 골짜기(손실 최소점)에 도달

```
기울기 양수 → 오른쪽이 올라감 → w를 줄임 (왼쪽으로)
기울기 음수 → 오른쪽이 내려감 → w를 늘림 (오른쪽으로)
기울기 0   → 최저점 도달 → 수렴
```

### 학습률 η의 중요성

| η가 너무 크면 | η가 너무 작으면 | η가 적절하면 |
|-------------|-------------|------------|
| 최적점을 지나쳐 발산 | 수렴이 매우 느림 | 적절한 속도로 수렴 |
| 손실이 줄지 않고 진동 | 학습 시간 오래 걸림 | 안정적 학습 |

### 경사하강법의 변형

| 변형 | 설명 |
|------|------|
| **배치 GD** | 전체 데이터로 기울기 계산. 정확하지만 느림 |
| **확률적 GD (SGD)** | 데이터 1개씩 기울기 계산. 빠르지만 불안정 |
| **미니배치 GD** | 소규모 배치로 계산. 속도와 안정성의 균형. **실전에서 가장 많이 사용** |

### 연결 포인트

> 경사하강법은 2차시 이후 **모든 신경망 학습의 기반**이다.
> - 3차시 RNN/LSTM/Transformer의 학습도 경사하강법
> - 2차시 신경망의 **역전파(Backpropagation)**는 경사하강법에서 기울기를 효율적으로 계산하는 방법
> - 3차시의 **기울기 소실 문제**는 이 역전파 과정에서 발생하는 문제

---

## 6. 신경망 (Neural Network)

> 선형회귀/로지스틱 회귀는 **선형 관계**만 학습 가능. 더 복잡한 비선형 패턴을 학습하려면 **신경망**이 필요하다. 이것이 ML에서 DL로 넘어가는 지점이다.

### Shallow Network (얕은 신경망)

- **입력층 → 은닉층 1개 → 출력층** 구조
- 은닉층이 1개만 있는 신경망

```
x₁ --(w₁)--\
x₂ --(w₂)---→  Σ  → + b → 활성함수 → y
x₃ --(w₃)--/
```

### 한 층의 연산 (가장 중요한 수식)

```
y = σ(Wx + b)

W : 가중치 행렬 (각 입력의 중요도)
x : 입력 벡터
b : 편향 (bias, 기준값 이동)
σ : 활성함수 (비선형성 부여)
```

**단계별 이해**:
1. **Wx**: 입력에 가중치를 곱함 → "각 입력이 얼마나 중요한지" 반영
2. **+ b**: 편향 추가 → "전체 결과를 위아래로 조정"
3. **σ( )**: 활성함수 적용 → "선형 결과를 비선형으로 변환"

### 활성함수 (Activation Function)

선형 연산을 **비선형으로 바꿔주는 장치** — 이게 없으면 층을 아무리 쌓아도 결국 하나의 선형 변환

| 활성함수 | 수식 | 출력 범위 | 특징 |
|---------|------|----------|------|
| **ReLU** | max(0, x) | 0 ~ ∞ | 음수는 0, 양수는 그대로. 계산 빠르고 성능 좋음. **딥러닝 기본값** |
| **Sigmoid** | 1/(1+e⁻ˣ) | 0 ~ 1 | 확률 해석 가능. 단점: gradient vanishing. 로지스틱 회귀에서 봤던 함수 |
| **Tanh** | tanh(x) | -1 ~ 1 | Sigmoid보다 개선, 출력 중심이 0. 여전히 vanishing 문제 있음 |

### 왜 활성함수가 필수인가?

```
활성함수 없이 2층을 쌓으면:
y = W₂(W₁x + b₁) + b₂ = W₂W₁x + W₂b₁ + b₂ = W'x + b'

→ 결국 하나의 선형 변환! 층을 쌓는 의미가 없음
→ 활성함수가 비선형성을 넣어줘야 층을 쌓을수록 더 복잡한 패턴 학습 가능
```

### Shallow vs Deep

| Shallow (얕은) | Deep (깊은) |
|---------|------|
| 은닉층 1개 | 은닉층 여러 개 (2개 이상) |
| 복잡한 표현을 위해 **넓이**를 키워야 함 | **깊게 쌓아서** 복잡한 표현 학습 |
| 파라미터 수 폭증 | 같은 파라미터로 더 효율적으로 학습 |
| 단순한 패턴 학습에 적합 | 이미지/텍스트 등 복잡한 데이터에 강력 |

### 딥러닝의 학습 과정

```
1. 순전파 (Forward Pass): 입력 → 각 층 통과 → 예측값 출력
2. 손실 계산: 예측값과 실제값의 차이 (손실함수)
3. 역전파 (Backpropagation): 손실의 기울기를 출력층→입력층 방향으로 계산
4. 파라미터 업데이트: 경사하강법으로 가중치 갱신
5. 반복
```

> 역전파는 **체인 룰(chain rule)**을 사용하여 각 층의 기울기를 효율적으로 계산하는 방법이다. 이 과정에서 기울기가 점점 작아지는 것이 3차시에서 배울 **기울기 소실 문제**이다.

---

## 차시 연결 요약

> 2차시의 모델들은 **복잡도 순서**로 연결된다:
> - **선형회귀** (가장 단순) → 연속값 예측, RSS 최소화
> - **로지스틱 회귀** (선형+시그모이드) → 분류 문제, 우도 최대화
> - **신경망** (비선형 층 쌓기) → 복잡한 패턴 학습, 경사하강법으로 최적화
>
> 이 모든 모델의 학습은 "**손실함수 → 경사하강법**"이라는 공통 프레임워크를 따른다.
> 3차시에서는 이 신경망을 **순차 데이터(텍스트)**에 맞게 변형한 RNN, LSTM, Transformer를 배운다.
