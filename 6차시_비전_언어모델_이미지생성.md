# 6차시 - Vision-Language Models, 이미지 생성, AI 파운데이션 모델

> 6차시는 지금까지 배운 모든 것을 **통합**하는 차시다. 5차시의 CNN(이미지)과 3~4차시의 Transformer(텍스트)를 합친 **멀티모달 모델**, 그리고 이미지를 **생성**하는 모델, 이 모든 것을 가능하게 하는 **파운데이션 모델**의 개념과 생태계를 배운다.

## 1. AI 파운데이션 모델 (Foundation Model)

### 정의

**대규모 데이터**로 사전학습되어, 다양한 하위 작업에 **범용적으로 적용** 가능한 기반 모델. 2021년 스탠포드에서 제안한 개념.

### 기존 AI vs 파운데이션 모델

| | 기존 AI | 파운데이션 모델 |
|--|--------|--------------|
| **학습 방식** | 작업마다 모델을 새로 학습 | 한 번 대규모 사전학습 → 여러 작업에 활용 |
| **데이터** | 각 작업별 소규모 라벨 데이터 | 인터넷 스케일 비라벨 데이터 |
| **적용** | 하나의 작업에만 특화 | 파인튜닝/프롬프트로 다양한 작업 수행 |
| **비유** | 특정 과목만 공부한 학생 | 폭넓게 공부한 뒤 각 시험에 맞게 적응하는 학생 |

### 파운데이션 모델의 대표 사례

| 분야 | 모델 | 배운 차시 |
|------|------|----------|
| 자연어 (Encoder) | BERT | 3차시 |
| 자연어 (Decoder) | GPT, LLaMA | 4차시 |
| 이미지 (분류) | ViT (Vision Transformer) | 5차시 CNN의 Transformer 버전 |
| 이미지+텍스트 | CLIP, SigLIP | 6차시 (이번!) |
| 이미지 생성 | Stable Diffusion, DALL-E | 6차시 (이번!) |
| 멀티모달 | GPT-4V, Gemini | 6차시 |
| 코드 | Codex, StarCoder | 4차시 GPT 계열 |

### 핵심 특징 3가지

#### 1. 규모의 법칙 (Scaling Law)

- 모델 크기, 데이터 양, 컴퓨팅이 커질수록 **예측 가능하게** 성능 향상
- 로그 스케일로 꾸준히 올라감

```
모델 크기:     1B → 10B → 100B → 1T 파라미터
성능 (대략):   보통 → 좋음 → 매우 좋음 → 놀라운 수준
```

#### 2. 창발적 능력 (Emergent Ability)

- 작은 모델에는 없다가 일정 규모를 넘으면 **갑자기 나타나는** 능력
- 예: 산술 추론, 코드 생성, few-shot learning, 논리적 사고
- 4차시의 **In-Context Learning**이 대표적 창발적 능력

```
모델 크기 10B 이하: few-shot으로 분류 불가능
모델 크기 100B 이상: few-shot으로 분류 가능!
→ 점진적이 아니라 "갑자기" 생기는 능력
```

#### 3. 전이 학습 (Transfer Learning)

- 사전학습에서 배운 지식을 **새로운 작업에 전이**
- 3차시 BERT의 "사전학습 → 파인튜닝"이 전이 학습의 대표 예시
- 소량의 데이터로도 높은 성능 달성 가능

### ViT (Vision Transformer)

5차시에서 CNN으로 이미지를 처리했다면, **ViT는 Transformer로 이미지를 처리**하는 방법

```
이미지를 패치로 분할 → 각 패치를 토큰처럼 임베딩 → Transformer Encoder에 입력

예: 224×224 이미지 → 16×16 패치 196개 → 196개의 토큰 시퀀스
→ Transformer가 패치 간 관계를 Self-Attention으로 학습
```

| CNN | ViT |
|-----|-----|
| 지역적 연결 (작은 영역만 봄) | 전역적 참조 (모든 패치를 동시에 봄) |
| 계층적으로 receptive field 확장 | 처음부터 전체 이미지 참조 가능 |
| 귀납적 편향 강함 (지역성, 이동불변성) | 귀납적 편향 약함 → 대규모 데이터 필요 |
| 적은 데이터에서도 잘 작동 | 대규모 데이터에서 CNN을 능가 |

> ViT는 CLIP의 **Image Encoder**로 사용된다.

---

## 2. Vision-Language Models (VLM)

### 정의

**이미지와 텍스트를 함께 이해**할 수 있는 멀티모달 모델. 두 모달리티를 **하나의 공유 공간**에서 연결.

### 왜 필요한가?

| 기존 | VLM |
|------|-----|
| 이미지 모델(CNN/ViT)과 텍스트 모델(BERT/GPT)이 **따로** | 이미지와 텍스트를 **함께** 이해 |
| "이 이미지가 고양이인지 개인지" 분류는 가능 | "이 이미지에 대해 설명해줘"도 가능 |
| 사전 정의된 클래스만 분류 | **어떤 텍스트 설명이든** 매칭 가능 |

### CLIP (Contrastive Language-Image Pre-training)

OpenAI가 2021년 제안. VLM의 대표 모델.

#### 구조

```
이미지 → [Image Encoder (ViT/ResNet)] → 이미지 벡터
                                              ↕ 유사도 계산
텍스트 → [Text Encoder (Transformer)]  → 텍스트 벡터
```

| 구성 | 역할 | 사용 모델 |
|------|------|----------|
| **Image Encoder** | 이미지 → 고정 크기 임베딩 벡터 | ViT 또는 ResNet (5차시 CNN) |
| **Text Encoder** | 텍스트 → 고정 크기 임베딩 벡터 | Transformer (3차시) |
| **공유 임베딩 공간** | 이미지와 텍스트 벡터가 같은 공간에 존재 | — |

#### 학습 방법: 대조 학습 (Contrastive Learning)

**배치 내 이미지-텍스트 쌍**에서, 맞는 쌍은 가깝게, 틀린 쌍은 멀게 학습

```
배치 내 4개의 쌍:
이미지1: 강아지 사진    텍스트1: "a photo of a dog"
이미지2: 고양이 사진    텍스트2: "a photo of a cat"
이미지3: 자동차 사진    텍스트3: "a red car"
이미지4: 비행기 사진    텍스트4: "an airplane in the sky"

유사도 행렬 (학습 목표):
         텍스트1  텍스트2  텍스트3  텍스트4
이미지1    ✓높게    ✗낮게    ✗낮게    ✗낮게
이미지2    ✗낮게    ✓높게    ✗낮게    ✗낮게
이미지3    ✗낮게    ✗낮게    ✓높게    ✗낮게
이미지4    ✗낮게    ✗낮게    ✗낮게    ✓높게

→ 대각선(맞는 쌍)의 유사도를 최대화, 나머지(틀린 쌍)를 최소화
```

- 학습 데이터: 인터넷에서 수집한 4억 개의 (이미지, 캡션) 쌍
- 별도의 라벨링 불필요 → **자기지도 학습**에 가까움

#### CLIP의 활용: Zero-shot 분류

학습 시 본 적 없는 클래스도 **텍스트 설명만으로** 분류 가능

```
분류할 이미지: (새 이미지)

1. 이미지 → Image Encoder → 이미지 벡터

2. 후보 클래스들을 텍스트로 변환:
   "a photo of a dog" → Text Encoder → 벡터A
   "a photo of a cat" → Text Encoder → 벡터B
   "a photo of a bird" → Text Encoder → 벡터C

3. 이미지 벡터와 각 텍스트 벡터의 유사도(코사인) 계산:
   유사도(이미지, A) = 0.85
   유사도(이미지, B) = 0.12
   유사도(이미지, C) = 0.03

4. 가장 높은 유사도 → "dog" 분류
```

> 놀라운 점: 학습 데이터에 "dog"이라는 라벨이 **명시적으로** 있었던 것이 아님. 이미지-텍스트 쌍에서 **자연스럽게** 개념을 학습.

#### CLIP의 다른 활용

| 활용 | 방법 |
|------|------|
| **이미지 검색** | 텍스트 쿼리로 가장 유사한 이미지를 검색 |
| **이미지 캡셔닝** | (다른 모델과 결합하여) 이미지에 대한 설명 생성 |
| **멀티모달 이해** | 이미지와 텍스트의 관계 파악 |
| **이미지 생성 가이드** | Diffusion 모델에서 텍스트 조건으로 사용 (DALL-E 2) |

### 연결 포인트

> CLIP = 5차시의 **CNN/ViT(이미지)** + 3차시의 **Transformer(텍스트)** + **대조 학습**
> 4차시의 Zero-shot/Few-shot 개념이 **텍스트에서 이미지 영역으로 확장**된 것

---

## 3. SigLIP (Sigmoid Loss for Language-Image Pre-training)

### CLIP과의 관계

SigLIP은 CLIP의 **손실 함수를 개선**한 모델. 아키텍처는 같고, 학습 방법이 다르다.

### CLIP의 한계: Softmax 대조 손실

CLIP은 배치 내 유사도를 계산할 때 **Softmax**를 사용:

```
CLIP의 손실 (Softmax):
각 이미지에 대해, 맞는 텍스트의 유사도가
"배치 내 모든 텍스트" 대비 상대적으로 높아야 함

→ 배치 내 모든 쌍을 비교해야 함 (정규화)
→ 배치 크기가 클수록 "틀린 쌍"이 많아져 학습이 잘됨
→ 작은 배치에서는 성능 저하
```

| CLIP의 문제 | 설명 |
|------------|------|
| **큰 배치 필수** | 배치 크기 32,768 사용. 엄청난 GPU 메모리 필요 |
| **상대적 비교** | 배치 구성에 따라 학습이 달라짐 |
| **계산 비용** | 배치 내 모든 쌍의 유사도를 계산해야 함 |

### SigLIP의 개선: Sigmoid 손실

각 (이미지, 텍스트) 쌍을 **독립적으로 이진 분류**

```
SigLIP의 손실 (Sigmoid):
각 (이미지, 텍스트) 쌍에 대해:
  - 맞는 쌍 → σ(유사도) = 1에 가깝게 (positive)
  - 틀린 쌍 → σ(유사도) = 0에 가깝게 (negative)

→ 각 쌍을 독립적으로 판단 (다른 쌍과 비교할 필요 없음)
```

### Softmax vs Sigmoid 비교

| | CLIP (Softmax) | SigLIP (Sigmoid) |
|--|----------------|-----------------|
| **비교 방식** | 배치 내 전체 쌍 **상대적** 비교 | 각 쌍을 **독립적** 판단 |
| **배치 크기 의존** | 큰 배치 필수 (32K) | 배치 크기에 덜 민감 |
| **계산** | 정규화 필요 (expensive) | 이진 분류 (cheap) |
| **직관** | "이 배치에서 가장 맞는 쌍은?" | "이 쌍이 맞나 틀리나?" |
| **확장성** | 배치 키우기 어려움 | 더 쉽게 확장 가능 |

### 비유로 이해하기

```
CLIP (Softmax):
  시험에서 "이 4개 보기 중 정답은?" → 상대 비교 (보기가 많을수록 잘 맞춤)

SigLIP (Sigmoid):
  각 보기마다 "이것이 맞는가? O/X" → 독립 판단 (보기 수에 덜 의존)
```

### 성능

- SigLIP은 CLIP과 **비슷하거나 더 나은 성능**을 더 **효율적으로** 달성
- Google에서 제안 (2023)
- 이후 여러 VLM (PaLI, Gemini 등)에서 SigLIP을 Image Encoder로 사용

> SigLIP = "CLIP의 대조 학습을 Sigmoid 손실로 단순화·효율화한 모델"

---

## 4. 이미지 생성 모델

### 이미지 생성의 목표

학습 데이터의 **확률 분포를 학습**하여, 그 분포에서 샘플링하여 **새로운 이미지를 생성**

```
학습 데이터: 수백만 장의 고양이 사진
모델이 학습하는 것: "고양이 사진의 확률 분포"
생성: 그 분포에서 새로운 고양이 사진을 "꺼냄"
```

### 주요 생성 모델 3종

#### GAN (Generative Adversarial Network, 2014)

**두 네트워크가 서로 경쟁**하며 학습하는 적대적 학습

```
┌─────────────┐        ┌──────────────┐
│  Generator  │ ──────→│Discriminator │
│  (생성자)    │  가짜   │  (판별자)     │──→ 진짜/가짜?
└─────────────┘  이미지  └──────────────┘
       ↑                       ↑
  랜덤 노이즈              진짜 이미지
```

| 구성 | 역할 | 목표 |
|------|------|------|
| **Generator** | 랜덤 노이즈 → 가짜 이미지 생성 | 판별자를 속이는 이미지 만들기 |
| **Discriminator** | 입력 이미지가 진짜인지 가짜인지 판별 | 진짜/가짜를 정확히 구분하기 |

- 비유: 위조범(Generator)과 감정사(Discriminator)의 대결
- 서로 경쟁하며 점점 실력이 향상 → 결국 진짜와 구분 못할 이미지 생성

| 장점 | 단점 |
|------|------|
| 고화질 이미지 생성 | **모드 붕괴(mode collapse)**: 다양성 부족, 비슷한 이미지만 생성 |
| 생성 속도 빠름 (한 번의 forward pass) | **학습 불안정**: Generator와 Discriminator의 균형 맞추기 어려움 |
| 선명한 이미지 | 학습 실패 시 발산 가능 |

#### VAE (Variational Autoencoder, 2013)

**확률적 인코더-디코더** 구조. 데이터를 잠재 공간(latent space)의 **확률 분포**로 압축.

```
입력 이미지 → [Encoder] → 잠재 분포 (μ, σ) → 샘플링 → z → [Decoder] → 복원 이미지

학습: 입력 ≈ 출력이 되도록 (복원 손실 + 잠재 분포 정규화)
생성: 잠재 공간에서 z를 랜덤 샘플링 → Decoder → 새 이미지
```

| 구성 | 역할 |
|------|------|
| **Encoder** | 이미지 → 잠재 공간의 확률 분포 (평균 μ, 분산 σ) |
| **Reparameterization** | 분포에서 미분 가능하게 샘플링 (z = μ + σ × ε) |
| **Decoder** | 잠재 벡터 z → 이미지 복원/생성 |

| 장점 | 단점 |
|------|------|
| 학습이 안정적 | 생성 이미지가 흐릿할 수 있음 (blurry) |
| 잠재 공간이 연속적 → 보간(interpolation) 가능 | GAN보다 선명도 낮음 |
| 수학적 기반이 탄탄 | |

```
잠재 공간에서의 보간 (VAE의 장점):
고양이 벡터 z₁ ─────── 개 벡터 z₂
           │ 중간 값들 │
     고양이 같은 개? 개 같은 고양이?
→ 부드러운 변환 이미지 생성 가능
```

#### Diffusion Model (확산 모델, 2020~) — 현재 주류

**점진적 노이즈 추가/제거** 과정을 학습. 현재 가장 고품질의 이미지 생성 모델.

```
Forward Process (훈련 시):
원본 이미지 → +노이즈 → +노이즈 → +노이즈 → ... → 순수 노이즈
(x₀)         (x₁)      (x₂)      (x₃)           (x_T)

Reverse Process (생성 시 - 이것을 학습):
순수 노이즈 → -노이즈 → -노이즈 → -노이즈 → ... → 깨끗한 이미지
(x_T)         (x_{T-1})  (x_{T-2})              (x₀)
```

| 단계 | 설명 |
|------|------|
| **Forward process** | 원본에 단계적으로 가우시안 노이즈 추가 (정해진 스케줄) |
| **학습 목표** | 각 단계에서 **추가된 노이즈를 예측** (노이즈 예측 네트워크) |
| **Reverse process** | 노이즈에서 시작 → 학습된 네트워크로 노이즈를 단계적 제거 → 이미지 복원 |
| **생성** | 랜덤 노이즈에서 reverse process 실행 → 새 이미지! |

#### 텍스트 조건부 생성 (Text-to-Image)

CLIP 같은 텍스트 인코더를 사용하여 **텍스트 설명에 맞는 이미지**를 생성

```
"a cat wearing a hat" → [Text Encoder (CLIP)] → 텍스트 벡터
                                                      ↓
랜덤 노이즈 → [Diffusion Reverse Process + 텍스트 조건] → 모자 쓴 고양이 이미지
```

- 대표: DALL-E 2, Stable Diffusion, Midjourney, DALL-E 3
- **CLIP이 텍스트-이미지 연결 다리** 역할을 함

#### Stable Diffusion의 구조

```
텍스트 → [CLIP Text Encoder] → 텍스트 임베딩
                                      ↓
랜덤 노이즈 → [U-Net (잠재 공간에서 노이즈 제거)] → 잠재 벡터 → [VAE Decoder] → 이미지
```

- **잠재 공간(Latent Space)**에서 Diffusion 수행 → 계산 효율성 향상
- VAE의 Encoder/Decoder + Diffusion의 노이즈 제거 + CLIP의 텍스트 이해
- → 여러 기술의 **융합**

### 생성 모델 비교

| | GAN | VAE | Diffusion |
|--|-----|-----|-----------|
| **핵심 원리** | 생성자-판별자 경쟁 | 잠재 분포 학습+복원 | 노이즈 추가/제거 |
| **품질** | 높음 (선명) | 중간 (흐릿할 수 있음) | **매우 높음** |
| **다양성** | 낮을 수 있음 (mode collapse) | 높음 | **높음** |
| **학습 안정성** | 불안정 | 안정적 | **안정적** |
| **생성 속도** | **빠름** (1회 추론) | 빠름 | 느림 (수십~수백 스텝) |
| **조건부 생성** | 가능하지만 어려움 | 가능 | **매우 잘됨** (text-to-image) |
| **대표 서비스** | StyleGAN (얼굴 생성) | — | DALL-E, Midjourney, SD |
| **연도** | 2014 | 2013 | 2020~ |

> 현재 이미지 생성 분야는 **Diffusion 모델**이 주류. GAN의 선명함 + VAE의 안정성 + 조건부 생성의 유연성을 모두 갖춤.

---

## 5. 허깅페이스 (Hugging Face)

### 정의

AI/ML 모델, 데이터셋, 도구를 공유하는 **오픈소스 플랫폼 및 라이브러리 생태계**. "AI의 GitHub"라고도 불림.

### 핵심 구성 요소

| 구성 요소 | 설명 | 비유 |
|----------|------|------|
| **Transformers** | 사전학습 모델을 쉽게 로드/사용하는 Python 라이브러리 | AI 모델용 npm |
| **Model Hub** | 100만+ 사전학습 모델 저장소 | AI 모델의 GitHub |
| **Datasets** | 15만+ 공개 데이터셋 허브 | 데이터의 GitHub |
| **Spaces** | 모델 데모를 웹으로 배포 (Gradio, Streamlit) | AI 모델의 Vercel |
| **Tokenizers** | 빠른 텍스트 토크나이징 라이브러리 | — |
| **Diffusers** | Diffusion 모델 전용 라이브러리 | — |

### 왜 중요한가?

파운데이션 모델 시대에 **핵심 인프라** 역할:

1. **접근성**: 누구나 BERT, GPT-2, Stable Diffusion 등을 코드 몇 줄로 사용 가능
2. **재현성**: 논문의 모델을 다운로드하여 바로 실험 가능
3. **생태계**: 모델 + 데이터 + 학습 도구 + 배포까지 원스톱
4. **커뮤니티**: 오픈소스 기여와 모델 공유 문화

### 사용 예시

```python
# 감정 분석 — 2줄로 BERT 기반 분류
from transformers import pipeline
classifier = pipeline("sentiment-analysis")
result = classifier("I love this movie!")
# → [{'label': 'POSITIVE', 'score': 0.9998}]

# 이미지 생성 — Stable Diffusion
from diffusers import StableDiffusionPipeline
pipe = StableDiffusionPipeline.from_pretrained("stabilityai/stable-diffusion-2")
image = pipe("a cat wearing a hat").images[0]

# Zero-shot 이미지 분류 — CLIP
from transformers import pipeline
classifier = pipeline("zero-shot-image-classification")
result = classifier("cat.jpg", candidate_labels=["cat", "dog", "bird"])
```

### Hugging Face와 다른 차시 개념의 연결

| 차시 | 모델/개념 | Hugging Face에서 |
|------|----------|-----------------|
| 3차시 | BERT | `bert-base-uncased` 모델 |
| 4차시 | GPT, LLM | `gpt2`, `meta-llama/Llama-3` |
| 5차시 | CNN, ViT | `google/vit-base-patch16-224` |
| 6차시 | CLIP | `openai/clip-vit-base-patch32` |
| 6차시 | Diffusion | `stabilityai/stable-diffusion-2` |

> 허깅페이스 = "AI 모델과 데이터셋을 공유·활용하는 오픈소스 생태계. 파운데이션 모델의 민주화를 이끄는 핵심 플랫폼."

---

## 6. 딥러닝 학습 전략

> 좋은 모델 구조 ≠ 우수한 성능. 학습 **전략**(초기화, 정규화, 학습률 조정 등)이 성능을 크게 좌우한다.

### 활성화 함수 (2차시 보충)

| 함수 | 수식 | 출력 범위 | 특징 |
|------|------|----------|------|
| **Sigmoid** | 1/(1+e⁻ˣ) | 0~1 | 기울기 소실. 출력 중심이 0이 아님 |
| **ReLU** | max(0, x) | 0~∞ | 빠르고 효과적. **죽은 뉴런(Dead Neuron)** 문제 |
| **Leaky ReLU** | x>0이면 x, x≤0이면 αx (α≈0.01) | -∞~∞ | ReLU의 죽은 뉴런 문제 해결. 음수에도 작은 기울기 |
| **GELU** | x·Φ(x) | -∞~∞ | Transformer에서 주로 사용. 부드러운 ReLU |

```
ReLU의 Dead Neuron 문제:
  입력이 음수 → 출력 0, 기울기도 0 → 해당 뉴런이 영원히 업데이트 안 됨

Leaky ReLU 해결:
  입력이 음수 → 출력 0.01x, 기울기 0.01 → 작지만 살아있음
```

### 데이터 전처리

모델에 데이터를 넣기 전에 **형식과 조건을 통일**하는 과정

| 전처리 | 설명 | 이유 |
|--------|------|------|
| **데이터 형식 통일** | 모든 입력을 같은 자료형(float32 등)으로 변환 | 모델이 일관되게 처리 |
| **이미지 크기 통일** | 모든 이미지를 같은 크기(예: 224×224)로 리사이즈 | CNN/ViT는 고정 크기 입력 필요 |
| **정규화 (Normalization)** | 픽셀값을 0~1 또는 평균0/분산1로 스케일링 | 학습 안정성, 수렴 속도 향상 |
| **데이터 증강 (Augmentation)** | 회전, 반전, 크롭, 색상 변환 등 | 데이터 양 늘리기, 과적합 방지 |

```
정규화 예시:
  원본 픽셀: 0~255
  Min-Max: (x - 0) / (255 - 0) → 0~1
  표준화:  (x - μ) / σ → 평균 0, 분산 1

ImageNet 표준화 (가장 흔함):
  mean = [0.485, 0.456, 0.406]  (R, G, B)
  std  = [0.229, 0.224, 0.225]
```

### 가중치 초기화 (Weight Initialization)

모델 학습 시작 시 가중치를 **어떤 값으로 설정하느냐**가 학습 성패를 좌우

| 방법 | 설명 | 문제/특징 |
|------|------|----------|
| **모두 0으로** | 모든 가중치 = 0 | **절대 안 됨!** 모든 뉴런이 같은 값 출력 → 학습 불가 (대칭 문제) |
| **랜덤 초기화** | 작은 랜덤 값 | 깊은 네트워크에서 기울기 소실/폭발 가능 |
| **Xavier (자비에) 초기화** | 분산 = 2/(입력+출력 뉴런 수) | **Sigmoid, Tanh**에 적합. 입출력 분산을 유지 |
| **He (허) 초기화** | 분산 = 2/입력 뉴런 수 | **ReLU**에 적합. 음수를 0으로 만드는 것을 보정 |

```
Xavier: W ~ N(0, 2/(n_in + n_out))
  → Sigmoid/Tanh의 선형 구간에서 기울기가 잘 전달되도록 설계

He:     W ~ N(0, 2/n_in)
  → ReLU가 음수를 0으로 만들어 분산이 절반으로 줄어드는 것을 보정
```

> **ResNet은 He 초기화** 사용 (ReLU 기반이므로)

### 모델 정규화 (Regularization)

과적합을 방지하기 위해 모델 복잡도에 **제약을 거는** 방법 (1차시 오버피팅 해결법의 상세)

#### 가중치 감소 (Weight Decay) / L2 정규화

```
손실 = 원래 손실 + λ × Σ(wᵢ²)

λ: 정규화 강도 (하이퍼파라미터)
wᵢ: 각 가중치
```

- 가중치가 **너무 큰 값**을 가지는 것을 억제
- 모든 가중치가 골고루 작은 값을 갖도록 유도
- 모델을 **부드럽게** 만듦 → 과적합 방지

#### L1 vs L2 vs Elastic Net

| 방법 | 페널티 항 | 효과 |
|------|----------|------|
| **L1 정규화** | λ × Σ\|wᵢ\| | 일부 가중치를 **정확히 0으로** → 특성 선택 효과 (희소 모델) |
| **L2 정규화** | λ × Σwᵢ² | 가중치를 **작게** → 부드러운 모델 (가장 흔함) |
| **Elastic Net** | λ₁×Σ\|wᵢ\| + λ₂×Σwᵢ² | L1 + L2 결합. 둘의 장점을 동시에 |

#### 드롭아웃 (Dropout)

학습 중 각 뉴런을 **확률 p로 랜덤하게 비활성화**

```
학습 시:
  [O] [X] [O] [O] [X] [O]  (X = 드롭아웃된 뉴런, 매 배치마다 다름)

추론 시:
  [O] [O] [O] [O] [O] [O]  (모든 뉴런 활성, 가중치에 (1-p) 곱)
```

| 장점 | 단점 |
|------|------|
| 과적합 방지 효과 우수 | 학습 시간 증가 (매번 다른 서브네트워크) |
| **앙상블 효과**: 매번 다른 서브네트워크를 학습하는 것과 유사 | 하이퍼파라미터 p 튜닝 필요 |
| 구현 간단 | 추론 시 스케일링 필요 |

### 학습률 스케줄링

2차시에서 학습률(η)이 너무 크면 발산, 너무 작으면 느리다고 배웠다. 실전에서는 학습 중에 **학습률을 동적으로 변경**한다.

| 스케줄 | 동작 | 특징 |
|--------|------|------|
| **계단식 (Step Decay)** | 일정 에폭마다 학습률을 **비율로 줄임** (예: 30에폭마다 ×0.1) | 간단, 전통적 |
| **코사인 (Cosine Annealing)** | 코사인 함수 형태로 **부드럽게 감소** | 부드러운 수렴, Transformer에서 선호 |
| **선형 (Linear Decay)** | 선형으로 일정하게 감소 | 간단, 예측 가능 |
| **워밍업 (Warmup)** | 처음에 0에서 **천천히 올린 후** 감소 시작 | Transformer 필수. 초기 불안정 방지 |

```
워밍업 + 코사인 (Transformer 표준):

학습률
  │    /‾‾‾‾‾\
  │   /        ‾‾‾‾\_____
  │  /                    ‾‾‾___
  │ /
  └─────────────────────────────→ 에폭
  워밍업   코사인 감소
```

### 파라미터 vs 하이퍼파라미터

| | 파라미터 (Parameter) | 하이퍼파라미터 (Hyperparameter) |
|--|---------------------|------------------------------|
| **정의** | 모델이 학습하는 값 | 사람이 사전에 설정하는 값 |
| **결정 방법** | 경사하강법으로 자동 학습 | 사람이 직접 설정 / 탐색 |
| **예시** | 가중치(W), 편향(b), 필터 값 | 학습률, 배치 크기, 층 수, 드롭아웃 비율 |
| **개수** | 수백만~수조 개 | 수십 개 |
| **변경 시점** | 학습 중 매 스텝마다 | 학습 전에 결정 (또는 스케줄링) |

> **하이퍼파라미터 튜닝**: Grid Search, Random Search, Bayesian Optimization 등으로 최적값 탐색

---

## 7. 전체 커리큘럼 연결 맵

```
1차시: ML 기초 (회귀, 분류, 평가)
  ↓
2차시: 모델과 학습 (선형회귀, 로지스틱, 신경망, 경사하강법)
  ↓                          ↓
3차시: NLP (RNN→LSTM→Transformer→BERT)    5차시: 이미지 (CNN)
  ↓                                            ↓
4차시: LLM (Decoder/GPT, ICL, 디코딩, 선호학습)   │
  ↓                                            ↓
  └──────────────── 6차시: 통합 ──────────────────┘
                    ├── 파운데이션 모델 (모든 것의 큰 그림)
                    ├── VLM/CLIP (텍스트 + 이미지 연결)
                    ├── SigLIP (CLIP 개선)
                    ├── 이미지 생성 (GAN, VAE, Diffusion)
                    └── 허깅페이스 (생태계/인프라)
```

### 핵심 관통 개념

| 개념 | 등장 차시 |
|------|----------|
| **손실함수 + 경사하강법** | 2차시에서 시작 → 모든 모델 학습의 기반 |
| **기울기 소실 → 해결** | RNN→LSTM(3차시), CNN→ResNet(5차시) |
| **Attention** | Seq2Seq(3차시) → Transformer(3차시) → BERT/GPT(3~4차시) → CLIP(6차시) |
| **사전학습 → 파인튜닝/프롬프트** | BERT(3차시) → GPT/ICL(4차시) → CLIP zero-shot(6차시) |
| **Encoder vs Decoder** | Transformer(3차시) → BERT vs GPT(4차시) → VLM(6차시) |
| **대조 학습** | CLIP(6차시) → SigLIP(6차시) |

---

## 차시 연결 요약

> 6차시는 전체 과정의 **집대성**이다:
>
> - **파운데이션 모델**: 1~5차시에서 배운 모든 모델(BERT, GPT, CNN/ViT)의 공통 패러다임
> - **CLIP/SigLIP**: 3차시 Transformer(텍스트) + 5차시 CNN/ViT(이미지) = 멀티모달
> - **이미지 생성**: 2차시 신경망 학습 + 잠재 공간 + 조건부 생성의 융합
> - **허깅페이스**: 이 모든 모델을 **실전에서 사용**하기 위한 인프라
>
> AI 기초 이론의 큰 그림: **데이터(1차시) → 모델(2~5차시) → 응용과 생태계(6차시)**
